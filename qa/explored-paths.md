# QA Explored Paths

## Scénarios cross-rôle (API live) — QA-CROSS-ROLE

Table de suivi distincte du sweep URL ci-dessous : couvre des flux métier bout-en-bout
entre rôles testés directement contre l'API live (preuve = requête/réponse HTTP réelle,
root-cause dans le code avant tout finding). Voir issues `qa:auto` non liées à une route
front pour le détail.

| scénario | last_check | last_status | brief |
| --- | --- | --- | --- |
| B12/cloisonnement — patient réserve un praticien avec le slot_id d'un AUTRE praticien (cross-provider/cross-cabinet) + re-sweep RBAC écritures secrétariat + draft-quote hiding + FIFO 3-vues (NOUVEAU) | 2026-07-12T15:21Z | bug | **1 finding NOUVEAU (backend scope:rust), distinct de #3637.** **#3650 (P1, intégrité/double-booking)** — `create_appointment` chemin patient (`appointments.rs:1655-1662`) résout le slot `WHERE id=$1` **sans garde `provider_id`/`cabinet_id`/`status`** ; `practitioner_id`/`cabinet_id` viennent de `body.provider_id` (:1543-1558), pas du slot. Prouvé live E2E : patient (Marc Dubois) `POST /appointments {provider f0..f1 Hugo Marin/Cabinet Lyon, slot_id=8443e4f7 appartenant à Karim Haddad 90de..0010}` → **201** RDV 283c0336 chez Hugo Marin à l'heure du slot de Karim (2026-07-24T09:30) ; slot de Karim **reste OPEN** (non consommé, `UPDATE … WHERE id=slot AND cabinet_id=cabinetLyon`→0 lignes :1713-1722) → double-booking d'un créneau tiers. Reproduit 2× (d22c5628, 8443e4f7). **Insight** : un fix minimal de #3637 (`AND status='open'`) NE corrige PAS ce cas (le slot est open) — garde distincte requise. Contraste jumeau cabinet `create_cabinet_appointment` (`scheduling.rs:920-940`, filtre cabinet+status='open', practitioner dérivé DU slot). RDV de test annulés. **CLEAN ce run (audités, ne pas re-auditer bientôt)** : draft-quote hiding (migration 0134/#3487 LIVE — devis draft créé praticien→patient `GET /billing/quotes/:id`→404, `?status=draft`→[], `/quotes/:id`→404) ; RBAC écritures secrétariat (cabinet/quotes 403, prescriptions 403, medical-record 403, dental-chart 403, waiting-room/call-next 403 ; consultations POST=405 création implicite) ; today-notes cloisonnement (praticien 200 initiales, secrétaire 403) ; FIFO cohérent 3-vues (queue patient `appointments.rs:1381` / waiting-room `scheduling.rs:531` / call-next `scheduling.rs:343` tous `checkin_at ASC`) ; provider availability filtré open+online+futur (`marketplace.rs:849-857`) ; search filtres tous whitelistés (`marketplace.rs:520-594`) ; devices scopé user+soft-delete (`devices.rs`) ; reviews author anonymisé "Prénom I." (`reviews.rs:130`) ; implant-passport RLS 0077 ; notifications list all-read paginé. **NON prouvable** : bug qty `get_treatment_plan` (`treatment_plans.rs:289` somme unit sans qty) — seeds qty=1 partout (le "×2" est dans le libellé pas dans qty), latent non observable. Deploy sain (0134 live). Pharma creds env absentes → scénarios token-pharma skippés ce run. |
| B12 RDV cas-limites (patient réserve créneau bloqué) + B7 filtre `from`/`to` slots + B6 dependents CRUD/validation + B4 treatment-plans + B8 notif-preferences/notifications + B9 clinical conv guard (NOUVEAU) | 2026-07-12T14:20Z | bug | **3 findings NOUVEAUX + 1 dupe re-confirmé.** **#3637 (P1, régression #3614)** — `create_appointment` chemin patient (`appointments.rs:1655-1662`) résout le slot par id **sans garde `status='open'`** (ni cabinet/practitioner/futur), contrairement au jumeau cabinet `create_cabinet_appointment` (`scheduling.rs:920`). Prouvé live E2E : secrétaire crée slot **blocked** (71b08cbf, 2026-08-15T09:00) → patient `POST /appointments {provider f0..f1, slot_id}` → **201** RDV requested 488286b7 sur créneau indispo (GET patient confirme). #3614 affirmait « le chemin patient est correctement bloqué » → FAUX. **#3639 (P1, régression #3365, scope:flutter-front)** — écran « créneaux réservables » secrétariat vide : `bookable_slots_bloc.dart:65` envoie `from: DateTime.now()` (local, `isUtc=false`) → `slots_api.dart:19` `toIso8601String()` **sans Z** → backend `list_cabinet_slots` (`scheduling.rs:1811`) parse `DateTime<Utc>` strict → `.ok()`=None → **filtre ignoré silencieusement**. Prouvé live : `?from=2026-07-13T00:00:00.000` (sans Z)→200 créneaux dès 2020 (tous passés) ; `…Z`→4 créneaux corrects. `sanitizeBookableSlots` masque les passés → écran vide alors que créneaux futurs existent. **#3636 (P3)** — `post_account_dependents` (`auth/mod.rs:3458`) + `patch_account_dependent` (`:3607`) n'valident jamais `first_name`/`last_name` : proche sans nom accepté. Prouvé live : `POST /account/dependents {first_name:"",last_name:"",relationship:"enfant"}`→201 persisté (GET liste les proches vides). **#3546 (P1) re-confirmé live** (dupe, non re-filé) : `POST /payments/intent` amount négatif(-5000)/nul(0)/>>total(99999999) → 201 persistés. **CLEAN ce run** : notif-preferences PATCH (persiste, type invalide→422, PUT→405) ; treatment-plans math (268000=6000+120000+142000, reste 141250) ; dependents DELETE (204, non-owned→404) + relationship enum FR + birth_date future→422 ; search/providers filtres tous appliqués (specialty 5, sector 8, teleconsult 8, pmr 13, accepts_new 17/0, q, languages) ; reviews moderate recompute rating_avg (`reviews.rs:345`, #3576 fixé) ; **clinical conversation guard** (secrétaire A direct-by-id sur conv clinical c1..04 → 404 R+W, praticien→200) ; documents list/detail/download patient→200 ; cabinet/members pract non-admin→403. **NON prouvable ce run** : audit messaging Candidate 1 (secrétaire A accès direct-by-id conv d'un patient rattaché au secrétariat B, `cabinet_messaging.rs:395/479/559` sans filtre `secretariat_id`) — toutes les conv patient_cabinet sont visibles au secrétaire A (aucune conv B-only cachée en seed) → non reproductible sans créer un provider/patient rattaché au secrétariat B. À retenter avec données B-only. |
| B10 auth sécurité (refresh rotation/replay, MFA enroll/verify, login rate-limit) + B6 coverage/card + B11 cabinet/members RBAC + idempotence bookings (NOUVEAU) + consent allow-list post-#3587 | 2026-07-12T13:23Z | bug | **2 findings NOUVEAUX (backend scope:rust), 2 surfaces jamais auditées sous cet angle.** **#3631 (P2, conformité RGPD)** — `put_account_consent` (`auth/mod.rs:2862`) : l'allow-list du fix #3587 `["soins","ia_scribe","marketing","partage_confrere"]` **omet `partage_pharmacie`**, finalité RÉELLE accordée par le système au send_prescription (`pharmacy/orders.rs:328`, `prescriptions.rs:522`). Prouvé live : `GET /account/consents` liste `partage_pharmacie` granted=true (2026-07-10), mais `PUT /account/consents/partage_pharmacie {granted:false}` → **422** (irrévocable) ; contrôle ia_scribe/marketing/partage_confrere→200. Viole docs/12:183 (« Donne/révoque ») + docs/07:113 (« révocable » RGPD 7.3). Distinct de #3587 (acceptait tout) et #3624 (`soins`→500 conflit contrainte seed). **#3632 (P2, réservation perdue)** — `create_booking` (`bookings.rs:83-86`) : lookup idempotence `WHERE cabinet_id=$1 AND idempotency_key=$2` **sans fingerprint** → jumeau non-corrigé de #3547/#3620. Prouvé live E2E : book slot A (07:00, key K)→appt d88f2180 ; book slot B (07:30, MÊME K)→renvoie **le même appt d88f2180 (slot A)**, aucun RDV créé sur slot B (`GET appointments` : RDV 07:00 présent, 07:30 absent). Même gap `create_cabinet_appointment` (`appointments.rs:1590`) + `reviews.rs:74` (non filés, notés dans #3632). Fix = fingerprint slot_id+motif→409, cf #3547. **CLEAN ce run (audités, ne pas re-auditer bientôt)** : **refresh rotation** (RT1→200 rotation, replay RT1→401 + révocation de chaîne prouvée : RT2 devient 401) ; **MFA** (enroll patient→403, pro→200 secret sans persistance, verify code invalide→422 ; NON enrôlé sur compte partagé = login enforce MFA `login.rs:139`, casserait les runs) ; **login rate-limit** actif (429 IP 5/60s + email) ; **coverage** (`auth/mod.rs:2402` GET/PATCH regime invalide→422, NSS masqué « 1 80 05 …78 », tiers_payant persiste) ; **coverage/card** (side invalide→422, EICAR AV→422, PNG valide→201 document_id+signed_url) ; **cabinet/members RBAC** (get_cabinet pract→200/patient→403, members list+POST+DELETE pract non-admin→403 `ProAdminClaims`) ; **search_providers filtres** tous appliqués (`marketplace.rs:577-594` q/specialty/sector/teleconsult/pmr/accepts_new/languages/bbox ; only `place` MVP-ignoré + `available=garbage` permissif→tous, front-contrôlé) ; **reviews** create garde appt honoré+RLS patient+UNIQUE (code) ; **consultation acts** amount i32 borné négatif+serde (pas d'overflow) ; **treatment-plans** math cohérente (268000=6000+120000+142000, reste 141250=total−amo−amc ; bug qty latent `treatment_plans.rs:289` NON prouvable live, seeds qty=1, déjà noté 07-10) ; **notifications** mark-read scopé (own→200 persiste, foreign/cross-rôle→404, read-all→200) ; **directions/preparation** OK (deeplink peuplé, parking string/pmr bool trivial) ; **#3597 FIXÉ** (message vide/whitespace→422) ; **#3618 FIXÉ déployé** (parse dentiste→specialty=null, 13 dentistes). Deploy sain (HEAD a70391c4, live≈main). Pharma creds env absentes→fallback seed jean.officine/f1. |
| cohérence compteurs cross-vue : dashboard patient vs liste conversations vs mark-read (NOUVEAU) + re-audit validation notif-prefs/referring-doctor + cross-rôle order status patient↔pharmacie + cabinet-info/reminders | 2026-07-12T12:50Z | bug | **1 finding NOUVEAU (backend scope:rust), surface compteur jamais auditée sous cet angle.** **#3626 (P2, donnée fausse cross-vue)** — `get_dashboard` (`dashboard.rs:97-99`) compte les messages non lus `sender_kind IN ('practitioner','secretary')` en **omettant `'pharmacist'`**, alors que la liste `/conversations` (`messaging.rs:106-109`) ET `mark_conversation_read` (`messaging.rs:470/485`) incluent les 3. Prouvé live : pharma f1 POST message fil `9e44…`→liste unread=1 mais dashboard `unread_messages=0` ; contrôle positif praticien POST→dashboard=1 (compte 1/2). Sous-comptage systématique dès qu'une pharmacie écrit au patient. Fix = ajouter `'pharmacist'` au filtre dashboard. **CLEAN ce run (audités, ne pas re-auditer bientôt)** : cabinet-info `/cabinets/:id/info` (address/geo/hours/access tous null — **déjà #3592 open**, dupe) ; reminders mock hardcodé (**déjà #3543 open**) ; montants dashboard to_pay/to_sign vs /payments+/quotes (même `*100`, cohérents) ; notif-prefs contrat front/back aligné + XOR referring-doctor 422/422 + provider existence gardée ; pharmacy order state machine (received→preparing→ready→picked_up + reject/cancel gardées atomiques+RLS, statuts cohérents patient↔pharmacie, `patient_display_name`="Marc D." PII-minimisée) ; stock/pharmacy-quotes state machines (RLS fail-closed sur GUC `app.current_pharmacy_id`/`app.patient_account_id`) ; callback-request (surfacé secrétariat `scheduling.rs:1122`, pas dead-end) ; implant_passport (RLS 0077). NB run concurrent 12:45Z a filé #3625/#3628 (waiting-list dates) — distincts de #3626. Deploy sain (HEAD depuis 16b8b086). |
| surfaces JAMAIS auditées : `account/referring-doctor` (médecin traitant) + `pharmacy/stock-requests` E2E cabinet↔pharmacie state-machine + `pharmacy/quotes` création/send pharmacien + `account/dependents` CRUD + validation-sweep waiting-list (NOUVEAU) | 2026-07-12T12:45Z | bug | **2 findings NOUVEAUX (backend scope:rust).** **#3625 (P3, donnée fausse/500 overflow)** — `POST /v1/pharmacy/quotes` (`pharmacy/quotes.rs:147` `.map(\|item\| item.qty * item.unit_price_cents).sum()`) ne borne NI `qty` NI `unit_price_cents` et n'utilise pas `checked_mul` : `qty=2^62,price=4` → wrap i64 → **201 avec total_cents:0** (devis persisté à total SILENCIEUSEMENT FAUX), `qty=2^62,price=3` → wrap négatif → viole `CHECK(total_cents>=0)` (mig 0127:20) → **500 internal_error** ; somme multi-lignes déborde aussi → 500. Prouvé live (token pharma f1). Fix = borner + `checked_mul/add`→422 (cf #3548 devis dentaire). **#3628 (P3, donnée incohérente cross-rôle)** — `POST /v1/waiting-list` (`waiting_list.rs:102-107`) stocke `start_date`/`end_date` (`Option<String>`) directement dans `desired_window` JSONB **sans parse ni check** : fenêtre inversée (`2026-08-01`→`2026-07-01`) et chaîne non-date (`\"banana\"`) → 201 active, **affichées telles quelles au secrétariat** (`GET /cabinet/waiting-list`). Distinct de #3577 (côté `offer`). Fix = parser + `end>=start` (cf `post_account_dependents` auth/mod.rs:3418). **CLEAN ce run (audités E2E, ne pas re-auditer bientôt)** : **referring-doctor** (`auth/mod.rs:2678/2737`) — exclusivité provider_id⊕free_name→422, unknown/non-listé provider→404, free_name whitespace→422, upsert remplace, RBAC pro→403 ; **pharmacy/stock-requests** state-machine complète (sent→accepted→fulfilled, fulfill-sur-sent/re-accept/reject-après-accept/cancel-après-accepted tous 409, cancel sent→cancelled, cloisonnement f1↔f2 accept=404 + absent liste RLS mig0125, empty/label/qty→422, unknown pharmacy→404, RBAC croisé 403, unknown id→404) ; **pharmacy/quotes** cycle pharmacien (create draft, patient NE voit PAS le draft RLS mig0127:61, send draft→sent, re-send→409, patient voit sent, validation empty/label/qty/neg-price→422, unknown order→404, patient sur endpoint pharma→403) ; **dependents CRUD** (create/get/patch/delete soft, bad relationship/future DOB→422 create+patch, unknown→404, double-delete idempotent→404, cross-account via account_guardianship) ; **reviews rating** borné 1-5 (code reviews.rs:15 + CHECK mig0009:90) ; **payments/intent** bad kind→422, huge amount unknown quote→404 ; **avatar** bad-b64→422 ; **device-tokens** = alias de /devices (pas de doublon). Pharma creds env absentes → fallback seed `jean.officine`/f1 (pharmacist). Deploy sain (HEAD 16b8b086). |
| surfaces sous-couvertes : `search/parse` NL (0 mention) + `payments/pharmacy-quote-intent` idempotence + `account/prescriptions` draft + audit clean devices/notifications/stock-request-RLS/reviews-public/search_slots/availability/cancel/pickup (NOUVEAU) | 2026-07-11T07:30Z | bug | **3 findings NOUVEAUX (backend scope:rust), 3 surfaces jamais auditées.** **#3618 (P2, donnée fausse)** — `POST /v1/search/parse {q:'dentiste'}` → `resolve_specialty` étape 3 profession (`marketplace.rs:1210-1219`, `JOIN specialty ... WHERE pr.label ILIKE '%dentiste%' ORDER BY s.label LIMIT 1`) collapse la profession Chirurgien-dentiste en **une seule** spécialité alphabétique (Implantologie) → `search_providers?specialty=…` renvoie **5/13** dentistes (8 omnipraticiens masqués, prouvé live), interpretation « Implantologie » factuellement fausse. Fix = laisser `specialty=null` pour une profession multi-spécialités. **#3620 (P2, donnée fausse silencieuse paiement)** — `create_pharmacy_quote_payment_intent` (`billing.rs:697`) fait `SELECT ... FROM idempotency_keys WHERE key=$1` **sans empreinte** (jumeau non corrigé de #3547 dont le fix `fingerprint` est sur `create_payment_intent` `billing.rs:502-532`) → même Idempotency-Key rejouée sur un autre devis d'officine renvoie le paiement du 1er : prouvé live, KEY sur quote A(700c)+quote B(890c) → payment_id identique lié à quote A/700c (le devis cabinet renverrait 409). **#3622 (P3, incohérence affichage)** — `list_account_prescriptions` (`prescriptions.rs:643`) n'a pas de filtre `status<>'draft'` (RLS 0109 = appartenance seule) → ordonnances draft (`document_id=null`) visibles dans `/account/prescriptions`, incohérent avec devis (billing.rs:1335 filtre draft, fix #3487) ; bénin : pas de fuite contenu, order draft→409. **CLEAN (audités, ne pas re-auditer bientôt)** : devices register (empty fcm_token→201 P3 noté, bad platform→422), notifications scoping (`AND app_user_id`), reviews public (`status='published'`), stock_request RLS (mig 0125 cabinet+pharmacy policies présentes), search_slots + get_provider_availability (online_booking+deleted_at+status='open'+now() OK), cancel_appointment (release slot + checked_in→no_show), pickup-token/pickup-scan (status='ready' guard), create_payment_intent cabinet (fingerprint OK), appt create (past starts_at→422), queue position vs waiting-room (mêmes filtres today/status). Pharmacie non testée ce run (creds `$CRED_PHARMACIE_*`/`$PHARMACY_ID` absentes de l'env). Deploy sain (HEAD 7f42a2f7 @06:43Z). |
| WebSocket temps-réel `/v1/ws` (cloisonnement canaux) + réservation walk-in cabinet (statut créneau) + carte praticien publique (next_slot_at/available) — 3 surfaces jamais auditées + re-vérif fixes #3604/#3606/#3608/#3609 (NOUVEAU) | 2026-07-11T06:35Z | bug | **3 findings NOUVEAUX (backend, scope:rust), 3 surfaces jamais auditées.** **#3612 (P1, cloisonnement clinique WS)** — `GET /v1/ws` jamais testé : la branche `("pro", …)` de `authorize_conversation` (`realtime/mod.rs:438`, `SELECT 1 FROM conversation WHERE id=$1 AND cabinet_id=$2`) n'a **ni filtre de scope ni rôle** (`WsSession` ne porte même pas `role`, l.102-121) → un **secrétaire** s'abonne au canal `conversation:<fil clinique>` (`c1000000-…-04`) et **reçoit en temps réel** `message_created`/`read` (message_id + sender_kind + timing), alors que le REST lui renvoie 404 (garde §07 §4.1 depuis #3596). Prouvé live E2E : subscribe secr→`subscribed` ; praticien POST message→201 ; client WS secr reçoit `{event:message_created, message_id:dfc14d3e…, sender_kind:practitioner}`. Jumeau WS non couvert du fix #3596 (métadonnées seules, pas le corps → P1 pas P0). **Corrigé & mergé en cours de run (PR #3613).** Contrôles sains : patient→autre fil (01/d5)=`forbidden`, patient→waiting_room=`forbidden`, pharma→autre pharmacie/clinical/waiting_room=`forbidden`, no/bad token→rejeté. **#3614 (P2, machine à états)** — `create_cabinet_appointment` (`scheduling.rs:888`) résout le créneau (`scheduling.rs:918`, `WHERE id=$1 AND cabinet_id=$2 AND deleted_at IS NULL`) **sans check `status`** puis force `UPDATE … SET status='booked'` (`scheduling.rs:968`) → un secrétaire pose un RDV walk-in sur un créneau **`blocked`** (indispo praticien détruite blocked→booked) OU sur un **`held`** (hold patient actif → patient book ensuite=409 slot_taken, réservation volée). Prouvé live : blocked→hold patient 404 (correct) MAIS create cabinet=201 ; held→create cabinet=201 puis patient book=409. Le chemin patient est protégé par `claim_and_hold_slot` (garde `status='open'`), le walk-in n'a pas d'étape hold → court-circuite. Edges OK (slot inconnu→404, patient inconnu→404, missing→422, patient token→403). **#3616 (P2, donnée fausse publique)** — `search_providers` `next_slot_at` (`marketplace.rs:563`) + filtre `?available` (`:535/:542`) filtrent `status='open'` **sans** `online_booking=true` ni `deleted_at IS NULL` → résidu non corrigé de #3608/#3609 (fixes 0140/0142 n'ont couvert QUE `search_slots`/`get_provider_availability`). Prouvé live public : slot non-online 2026-07-12→`next_slot_at` devient 07-12T08:00 (mais hold=404, absent de search_slots) ; DELETE→`next_slot_at` **toujours** 07-12T08:00 (créneau supprimé annoncé). Carte praticien annonce une prochaine dispo non réservable/inexistante. **RE-VÉRIF FIXES LIVE (deploy `19990`@05:43Z, live≈main)** : **#3604 FIXÉ** (reschedule libère l'ancien créneau : re-hold post-reschedule→200, avant 409) ; **#3606 FIXÉ** (hold expiré→re-hold 200 après TTL 5min, mig 0141) ; **#3608 FIXÉ** (slot non-online absent de search_slots + hold→404) ; **#3609 FIXÉ** (slot supprimé disparaît de search_slots) ; get_provider_availability porte bien les 3 filtres + LIMIT 50. Deploy sain. |
| projection publique marketplace des créneaux (filtres `online_booking`/`deleted_at`) + pharmacie déclarée cross-rôle + avatar/dependents/teleconsult (NOUVEAU) | 2026-07-11T05:30Z | bug | **2 findings NOUVEAUX, famille 'projection publique marketplace des créneaux' (backend, scope:rust) — jamais auditée avant.** **#3608 (P2)** le flag `online_booking` (US-M19) est **lu par AUCUNE requête** : `search_slots` (`marketplace.rs:367`) et `get_provider_availability` (`marketplace.rs:846`) filtrent `status='open'` mais jamais `online_booking`, et `claim_and_hold_slot` (mig 0120/0140) non plus → `PUT /cabinet/slots/:id/online` totalement inerte, tout créneau `open` (défaut `online_booking=false`) est publiquement recherchable ET holdable/réservable. Prouvé live : slot créé + `PUT /online {false}` → présent dans `GET /search/slots?provider_id=f1` **SANS token (public)** + patient `hold`→200. Viole `docs/11:108` (« disponibilités publiques … séparée du planning interne ») + `docs/12:352` (US-M19). Front ne câble jamais `/online` (0 hit) → le toggle n'existe pas côté UI, tous les créneaux `bookable_slots` exposés. **#3609 (P2)** un créneau **soft-deleted** (`DELETE /cabinet/slots` pose `deleted_at`, garde `status='open'`, `scheduling.rs:2052`) reste **affiché disponible dans la recherche PUBLIQUE** : `search_slots`/`get_provider_availability` omettent `deleted_at IS NULL` (que `list_cabinet_slots` a pourtant depuis #3550). Prouvé live : créer→présent /search/slots public→DELETE 204→**toujours présent** /search/slots→patient hold=**404** (fantôme/cul-de-sac). Fix #3550 (PR #3581) n'a couvert que la vue back-office + le hold path (mig 0140/0141), pas les endpoints publics. Les DEUX défauts touchent aussi `search_providers` `next_slot_at` (`marketplace.rs:560`) + filtre `?available=today/week` (`marketplace.rs:531-543`) — commenté sur les 2 issues pour un fix holistique. **CLEAN ce run** : **pharmacie déclarée cross-rôle** (`PUT /account/pharmacy` patient → `GET /cabinet/patients/:id/pharmacy` sec+prac cohérent ; unknown→404, random patient→404 anti-probe, d4 sans pharma→204, patient token→403) ; **avatar** (`PUT /account/avatar` bad-mime/empty/non-b64→422, valid PNG→204, GET→bytes) ; **dependents non-bookables** (appointments liés `patient_account_id`, pas de dependent_id — by-design) ; **teleconsult** = attribut de recherche seul (pas de session visio — MVP) ; **search_slots** groupe/dédupe par provider correctement (ordre first-slot ASC) ; `put_cabinet_provider_listing` admin-gated (non testable, non-admin creds). Deploy sain (deploy success `19..`@03:36Z, live≈main). |
| surfaces auth/context jamais testées (select-context, password reset, pro/verification, secretariat provisioning) + webhooks signature + page-param family + audit code handlers sous-couverts (NOUVEAU) | 2026-07-11T04:19Z | OK | **0 nouveau bug filable ce run (large sweep de surfaces jamais couvertes + audit code de ~15 handlers).** **`POST /auth/select-context` CLEAN** (`select_context.rs`) : propre cabinet 11111111→200 (JWT role=practitioner), cabinet inexistant→404, cabinet existant non-membre (Paris `90ca0000-…-01`)→**403 no_membership**, secretariat_id bidon→403 ; le 404 vs 403 dépend de l'existence réelle (Annecy `22222222` ABSENT du live via seed_e2e non chargé → 404, cohérent). **Password reset CLEAN (code)** : `forgot_password` toujours 204 (anti-énum §1.8), `reset_password` token inconnu→404 / expiré→410 / policy password avant lookup. **`cabinet_secretariats` mutations** (PATCH/:id, POST/:id/members, POST/:id/staff) = **403 role-check-first** pour prac ET sec (pas de fuite tenancy ; foreign secretariat_id→403 aussi) ; happy-path admin non testable (pas de compte admin). **`patch_cabinet_provider`** prac→200 (own row), sec→403 ; `PatchProviderBody`={bio,specialite,langues,pmr} → champs inconnus (`consultation_fee_cents:-500`) ignorés par serde (pas de persistance, pas de bug) ; langues:string/pmr:string→422. **`GET /pro/verification`** prac→200 (rpps verified), sec→403. **`GET /cabinet/secretariats`** prac+sec→200, patient→403. **cloisonnement `cabinet/patients`** : list=5 patients scopés, `get_cabinet_patient` foreign/random→404. **page-param family COMPLÈTE** : seuls `list_cabinet_quotes`(billing.rs, #3594) + `list_cabinet_slots`(scheduling.rs:1803) rejettent `?page`→400 ; `/cabinet/appointments`, `/cabinet/consultations`, `/cabinet/patients` ignorent page→200 ; slots non envoyé par le front → seul #3594 casse un écran (re-confirmé live : `?page=1`→400 unsupported_pagination_param, plain→200/37 devis). **webhooks CLEAN live** : stripe/yousign/gocardless sans sig→401, sig bidon→401, **forge clé-vide (HMAC empty-key sur body exact)→401** ⇒ secret configuré en prod (bypass NON exploitable live). **pharmacy-quote patient decision** : re-accept→409, refuse-après-accept→409, unknown→404, praticien→403 ; DTO liste porte bien `total_cents`/items (prix visible). **garbage validation** reviews(rating float→422, missing Idempotency-Key→400)/bookings(missing slot_id→422)/coverage-card(multipart→400) = propre. **Re-confirmé OUVERT non-filé** : **#3558** (patient reschedule date passée) — `patch_appointment` (`appointments.rs:78`) garde `too_late` sur le starts_at COURANT, UPDATE (l.91) COALESCE le nouveau starts_at SANS check `>now()` ni slot ; le fix #3604 a ajouté la libération de créneau mais PAS la validation → root cause inchangée sur main. **LATENTS CODE-ONLY non-filables (pas de preuve live possible ce run — notés pour un run futur avec compte admin / accès secret webhook)** : (a) **webhook empty-secret guard manquant** — `stripe/gocardless/yousign::verify_*` + `main.rs:15 jwt_secret` chargent le secret via `unwrap_or_default()` et ne rejettent pas une clé vide avant le MAC (`HmacSha256::new_from_slice(b"")` réussit) → si le secret env était vide, forge triviale ; **NON exploitable live (secret set, prouvé 401)** ; hardening à faire (P1 conditionnel). (b) **stripe status regression** — `webhooks/stripe.rs:247` `update_payment_status` UPDATE sans garde de statut, alors que gocardless (`AND status='pending'`) et yousign (`AND signed_at IS NULL`) gardent → un `payment_failed` tardif écraserait `paid`→`failed` ; non-reproductible sans le secret webhook. (c) **provision_staff** (`cabinet_secretariats.rs:488`) résout un compte par email sans filtre `kind='pro'` → un admin/manager pourrait attacher un compte patient à SON cabinet + oracle d'énumération 200(existe)/201(nouveau) ; admin-only → 403 pour nos creds, non-reproductible. Deploy sain (deploy success `19750`@03:36Z, live≈main). |
| slot lifecycle desync : reschedule ne libère pas l'ancien créneau (#3604) + hold expiré jamais libéré (#3606) + re-vérif fixes #3602/#3596/#3559/#3550/#3548 (NOUVEAU) | 2026-07-11T03:25Z | bug | **2 findings NOUVEAUX, famille 'slot lifecycle desync' (backend, scope:rust).** **#3604 (P2)** `PATCH /v1/appointments/:id` (reschedule patient) ne touche jamais le créneau : `patch_appointment` (`appointments.rs:32`, le SELECT l.55 ne lit même pas `slot_id`, l'UPDATE l.87-104 = seulement starts_at/ends_at/motif) → ancien créneau reste `booked` orphelin (indispo fantôme) + `appointment.slot_id` diverge de `starts_at`. Prouvé live : slot `510ad11c` booké → reschedule 07-19→07-22 → re-hold du 07-19 = **409 slot_taken** ; cancel du RDV (à 07-22) libère le 07-19 = **200** (prouve slot_id figé). Même gap `patch_cabinet_appointment` (`scheduling.rs:1498`). Contraste : cancel (`appointments.rs:261`) & booking (`bookings.rs:176`) gèrent le créneau. Distinct de #3558 (validation date). **#3606 (P1)** hold expiré JAMAIS libéré → créneau `held` à vie : `claim_and_hold_slot` (mig 0120) ne consulte pas `slot_holds.expires_at` ; hold DELETE seulement à la réservation (`bookings.rs:186`) ; aucun reaper (`JobDispatcher`=`StubJobDispatcher` no-op `lib.rs:88-93`, `main.rs` serveur unique) ; mig 0095 promet « expiration automatique … DELETE à l'expiration » **non implémentée**. Prouvé live : slot `d6fc88ec` hold 03:15:36 (TTL 5min, expire 03:20:35) → à 03:23 : book avec ancien token = **409 hold_invalid**, re-hold = **409 slot_taken** (créneau stérilisé, invisible car toutes les dispos filtrent `status='open'`). Chaque hold abandonné retire un créneau du funnel. Les deux filés scope:rust/rust-agent. **RE-VÉRIF FIXES LIVE (deploy 19948@03:22Z, live≈main)** : **#3602 FIXÉ** (secrétaire→documents=carte_mutuelle only, catégorie clinique explicite→vide ; Hugo non-suivant Camille→403 sur les 4 familles ; Hugo suivant Marc→200 avec cr/radio/ordonnance) ; **#3596 FIXÉ** (secrétaire GET/POST/read fil `scope=clinical` c1..04 = **404**, praticien 200) ; **#3559 FIXÉ** (Hugo→Camille medical-record/dental-chart/notes/documents = 403) ; **#3550 FIXÉ** (`list_cabinet_slots` filtre `deleted_at IS NULL`) ; **#3548 FIXÉ** (`create_cabinet_quote` rejette amount<=0 + deposit_pct∉[0,100]). **CLEAN vs design** : idempotency booking cross-compte **étanche** (patient B, même Idempotency-Key, autre slot → sa propre RDV distincte ; le lookup idempotence est RLS-scopé compte → PAS de fuite comme le risque code #3547) ; `patch_cabinet_slot` refuse un créneau `booked` (409) ; `patch/delete_consultation_act` gardés `in_progress` ; `sign_quote` exige `sent` ; `offer_waiting_list_slot` exige proposed_at>now+5min ; index unique 1 order actif/prescription (0124). **Re-confirmés OUVERTS (non re-filés)** : #3543 (reminders mockés hardcodés), #3592 (cabinet info clés null). |
| cloisonnement clinique §14 documents (4e endpoint famille #3559) + messagerie pharmacie cloisonnement (NOUVEAU) | 2026-07-11T02:07Z | bug | **1 finding NOUVEAU : #3602 (P1).** `GET /v1/cabinet/patients/:id/documents` (`list_patient_documents`, `clinical.rs:1030`) est le **4e endpoint clinique** cabinet→patient et n'a **ni garde §14** (relation de soin) **ni filtrage catégorie** — seulement `ProSecretaryPlusClaims` + check appartenance cabinet. Prouvé live : (A) praticien Hugo SANS RDV avec d4 (Camille) → notes/medical-record/dental-chart=**403** mais documents=**200** (autorisation accordée là où les 3 sœurs refusent) ; (B) **secrétaire** (403 sur tout le clinique) → documents de d1 (Marc)=**200** avec catégories `cr`(compte-rendu)/`radio`/`ordonnance` + filename/mime = **fuite métadonnées cliniques**. Défaut explicitement signalé dans le commentaire de #3559 (2026-07-10T14:43Z « le correctif §14 doit couvrir les quatre familles ») mais laissé HORS-scope du fix PR #3582 (n'a touché que medical_record+dental_chart). Sibling correct = `list_patient_notes` `clinical.rs:470-487` (`has_appointment` + `ProPractitionerClaims`). `upload_patient_document` (POST, `clinical.rs:1200`) a le même gap (write) → même root cause, couvert par le fix, pas de dupe. **Reste CLEAN** : **messagerie pharmacie cloisonnement** — `get_pharmacy_conversation_messages`/`mark_pharmacy_conversation_read` (`pharmacy/messaging.rs:132/272`) font `SELECT 1 FROM conversation WHERE id=$1` **sans** `AND pharmacy_id=$2` (contrairement au send l.212) MAIS RLS `conversation_pharmacy_all`/`message_pharmacy_all` (mig 0126) fail-closed couvre : token pharma f1 → GET messages d'un fil cabinet-clinical `c1..04`/patient-cabinet `a6d6..`/inexistant = **404** ; mark-read pharma = `UPDATE … sender_kind='patient'` (pas de bug UUID #3555). Deploy sain (dernier succès `19..`@01:13Z, live≈main). |
| flux devis signature/acompte (confirm_signature JAMAIS testé) + treatment-plans + dashboard agrégats + order state-machine + reviews recompute #3576 + self-service patient (NOUVEAU) | 2026-07-11T01:15Z | OK | **0 nouveau bug. Large couverture NOUVELLE, tout propre/by-design/déjà-filé.** **(1) Flux devis E2E** (praticien `create_cabinet_quote`→draft, secr `send`→sent, patient `sign`/`confirm_signature`→signed, `deposit`) prouvé cohérent : draft invisible patient (404, RLS `quote_patient_read_exclude_draft` 0134), sign sur draft→404, cross-vue `signed_at` cohérente cabinet↔patient, re-send signed→409, sign mauvais kind (praticien)→403, deposit sans Idempotency-Key→422 / avec→201 idempotent (même payment_id). `confirm_signature` (jamais couvert) = alias idempotent de `sign_quote`, OK. **Overpayment** : deposit 50000¢ sur devis total 10300¢→201 (aucun plafond, `create_payment_intent` `billing.rs:482` = 0 validation montant) → **même root cause que #3546 (open, négatif/nul)** → non re-filé (near-dupe). **(2) treatment-plans** GET-only patient : montants cohérents (items somment au total 268000, reste-à-charge 141250 = total−AMO−AMC), pro→403, inexistant→404. **(3) cloisonnement clinique** sweep secrétaire : medical-record/dental-chart/notes (R+W), today-notes, consultations, ccam/acts = **403 partout**, praticien 200 (`ProPractitionerClaims`) ; `/acts` catalogue partagé 200 OK. **(4) order pharmacie state-machine** patient : cancel received/preparing→cancelled, cancel picked_up/ready→409, pickup-token preparing→409/ready→200, inexistant→404. **(5) dashboard** agrégats = source-de-vérité (to_sign=2=devis sent, to_pay=25=paiements pending, unread=4 cabinet-only vs 5 conv incl. pharmacie = sémantique documentée). **(6) #3576 CONFIRMÉ FIXÉ LIVE** : modération avis published → `provider.rating_avg` recalculé 4.8/124 (seed figé) → 3.7/7 (moyenne réelle 7 publiés) ; prouve aussi **live≈main frais** (fix `6b813b21` servi). **(7) notification-preferences** roundtrip GET→PATCH partiel→GET persiste. **(8) search** parse=POST (GET→405), providers q=nom/spécialité (pas ville, by-design), slots `provider_id` param inexistant→ignoré (renvoie tous, by-design marketplace), specialty non-UUID→400. **(9) account/prescriptions** : draft visible patient (doc12:506 ancre policy 0109 sans filtre statut) = **conforme design, re-confirmé non-finding** ; guards order draft→409, inexistant→404. **(10) implant-passport** read+export(302)+RBAC(pro→403) OK. **(11) `/appointments?filter=<inconnu>`** tombe dans `_ => ""` (`appointments.rs`) → renvoie TOUS les RDV au lieu de 422/défaut — P3 parsing permissif, données propres du patient (RLS), front contrôle les valeurs → non-filé. **⚠️ `$CRED_PHARMACIE_*`/`$PHARMACY_ID` toujours absents** (PRIO 2 pharma-token non testable ce run ; contournable via seed `jean.officine` si besoin). |
| audit contrat front↔backend (pagination `page` vs `cursor`, méthode) + cloisonnement clinique messagerie + surfaces publiques (NOUVEAU) | 2026-07-11T00:14Z | bug | **6 findings NOUVEAUX.** **#3596 (P0 cloisonnement clinique)** : un **secrétaire** LIT et ÉCRIT les fils `scope='clinical'` par ID — `get_cabinet_conversation_messages`/`send_cabinet_message`/`mark_cabinet_conversation_read` (`cabinet_messaging.rs:379/446/532`) ne gardent que `WHERE id=$1 AND cabinet_id=$2`, **sans** filtre de scope, alors que `list_cabinet_conversations` bloque le secrétaire (403 sur `?scope=clinical` l.97-98, `AND c.scope != 'clinical'` l.118) ; aucune RLS DB sur scope. Prouvé live sur le fil seed `c1000000-…-04` (Marc Dubois) : secr `?scope=clinical`→403, fil absent de sa liste, MAIS GET `.../messages`→200 (contenu « SEED »), POST message→201 (`fdb64e78`), POST /read→204 ; praticien confirme 2 msgs. **#3594 (P1)** : la liste des devis cabinet ne charge JAMAIS — le front `CabinetQuotesApi.list({page})` (`cabinet_quotes_api.dart:15-17`) envoie toujours `?page=1` que `list_cabinet_quotes` (`billing.rs:1042`) **rejette en 400** (`UnsupportedPageParam`, migré vers limit/offset #3521) → `DevisError` permanent praticien+secrétariat (sans page = 200). **#3593 (P2)** : historique RDV patient tronqué aux 20 plus récents — `GET /appointments?filter=history&page=N` ignore `page` (backend pagine par `cursor`, `appointments.rs:446` sans champ `page` ; réponse `page.next_cursor`), front `getHistory({page})` (`scheduling_api.dart:68`) n'utilise jamais le cursor ; prouvé 110 RDV total, page1==page2==page3. **#3598 (P2)** : coffre-fort documents tronqué aux 20 (29 docs, front `DocumentApi.getAll` `document_api.dart:10` ignore `next_cursor`, `documents_bloc.dart:35`) — même famille. **#3592 (P3)** : `/cabinets/:id/info` (public US-P28) renvoie horaires/parking/accessibilité/door_code **tous null** malgré `settings={code_entree:"A12",horaires,parking:true,pmr:true}` — mauvaises clés JSON (`cabinet_info.rs:112-121` : `hours`≠`horaires`, `pmr_access`≠`pmr`, `parking` lu string vs bool, `door_code`≠`code_entree`) + `is_current_patient` calculé mais jamais utilisé pour gater door_code (fuite latente masquée). **#3597 (P3)** : patient poste des messages vides/whitespace (`POST /conversations/:id/messages {body:""}`→201) alors que le cabinet refuse (422) — validation absente `messaging.rs:555` vs `cabinet_messaging.rs:453`. **Latents non filés (code mort front)** : `CabinetQuotesApi.create`/`update` (toJson sans `items`→422) et `SchedulingApi.book` (sans `provider_id`→422) et `CabinetAppointmentsApi.getById` (route PATCH-only→405) — aucun écran ne les invoque. **Même pattern pagination sur `/notifications`** (front `getAll` ignore next_cursor, 20 max) — noté, non filé (mitigé par read-all). **#3545/#3557/#3575 re-confirmés FIXÉS live** (patient_name 149/150, directions deeplink peuplé, set_consultation_note round-trip+RBAC 403). **#3558 re-vérif inconclusive** (RDV même-jour → garde `too_late` masque le test). Deploy sain (dernier `deploy` succès `19820`@23:06Z, live≈main). |
| validation d'entrée écriture cabinet (prescription/quote patient_id) + sweep bad-ref 500 patient/cabinet + consent purpose (NOUVEAU) | 2026-07-10T23:05Z | bug | **2 findings NOUVEAUX.** **#3586 (P2)** : `POST /cabinet/prescriptions` et `POST /cabinet/quotes` ne valident **pas** `patient_id` — patient inconnu → **500 brut** (au lieu de 404/422 ; la seule barrière = FK `patient(id)` + RLS `cabinet_id`, aucune garde d'appartenance au cabinet, cf. le patron correct `add_patient_note` `clinical.rs:631-644`), et une **ligne d'ordonnance 100% vide** (`label/posology/duration=""`) → 201 persistée (`735f54c8`). Root cause `prescriptions.rs:54` (seul check `items.is_empty`) + `billing.rs:872`. **#3587 (P3)** : `PUT /account/consents/:purpose` accepte n'importe quelle chaîne comme finalité RGPD (`qa_garbage_purpose_xyz`→200, persistée, re-listée par GET ; ~10 finalités bidons déjà accumulées) — root cause `auth/mod.rs:2856` bind du path sans allow-list, table `consent_record` sans CHECK. **Reste = aucun écart** : sweep bad-ref sur **toutes** les écritures patient (appointments/bookings/conversations/waiting-list/slots-hold/dependents) → 404/422 propres ; écritures cabinet (consultations/acts/complete, stock-requests, dental-chart, medical-record, prescriptions/sign, documents) → 404/422 ; **cloisonnement patient↔patient** : Marc ordonne l'ordonnance de Camille (`735f54c8`)→404 (RLS `prescription_patient_read` 0109). |
| PRIORITÉ 2 pharmacie click-and-collect E2E (token pharma RÉEL seed `jean.officine`/f1) — cloisonnement + state machine + pickup (NOUVEAU) | 2026-07-10T23:05Z | OK | **Aucun écart, 0 finding.** Token pharma obtenu (login `jean.officine@pharmacie-lyon.test`/`Nubia2026!` → select-pharmacy-context f1, kind=pharma). **Token-kind separation** : pharma→`/cabinet/agenda`,`/dashboard`,`/account/prescriptions`=403 ; pro(praticien)→`/pharmacy/orders`=403 ; patient→`/pharmacy/orders`=403 ; pharma→`/pharmacy/orders`=200. **Cloisonnement inter-pharmacie** : Marc commande une ordonnance signée vers **f2** (`83ad5b0c` received) → f1 READ + ACCEPT dessus = **404** ; devis f1 sur order f2 = 404 ; liste `/pharmacy/orders` = uniquement pharmacy_id=f1 (16 cmd). **PII minimisée** : `patient_display_name="Marc D."`. **State machine** (`orders.rs:653+`, transitions atomiques `WHERE status=$expected`) : accept received→preparing 200, re-accept 409, reject sur preparing 409, ready preparing→ready 200, re-ready 409, accept sur ready 409 ; **pickup-scan** : patient pickup-token → pharma scan ready→picked_up 200, double-scan 409, patient voit `picked_up`. **Pharmacy quotes** : prix négatif→422, order hors tenant→404. |
| recherche pharmacie (accents) + PRIORITÉ 1 file-attente cycle complet + agenda cloisonnement + RBAC/cross-cabinet 22:00Z (NOUVEAU) | 2026-07-10T22:00Z | bug | P3 **#3578** : `GET /v1/pharmacies?q=rhone` (sans accent) → `{data:[]}` alors que `q=Rhône` renvoie « Pharmacie du Rhône » — root cause `pharmacy/directory.rs:70-72` filtre en `ILIKE` brut sans repli d'accents, alors que `search_ccam_acts` (`consultations.rs:953-954`, doc §12 « accent-insensible ») applique `translate()`. Reste = **aucun écart** : **PRIORITÉ 1 file-attente** cycle frais 2 RDV FIFO par `checkin_at` (RDV tardif checké en 1er passe devant) — positions `GET /queue` (1/2/3) et 3 vues (patient queue / secrétariat WR initiales `MD` / praticien WR nom complet) cohérentes, `call-next→start(#3477 fix RE-CONFIRMÉ live)→add-act→complete` sort bien le patient de la file, `cancel` d'un `checked_in`→`no_show` sort de la file, double-cancel/cancel `done`→409, dashboard `next_appointment` exclut le `no_show` ; **agenda cabinet** cloisonnement sain — un RDV Girard neuf (`create_appointment` dérive `cabinet_id` du provider, `appointments.rs:1536`) reste au « Cabinet Girard » et n'apparaît PAS dans l'agenda Lyon (le seul RDV orphelin résiduel `142536a7` = donnée pré-#3542, non reproductible, #3542 fermé) ; **RBAC** patient→cabinet/pharmacy 403, secrétaire→call-next/consultations 403, praticien→dashboard/account/appointments 403, prc→PATCH RDV d'un autre cabinet 404 ; notif-preferences PATCH→GET persiste ; preparation/directions/treatment-plans/parse OK. Déjà filés (skip) : #3558 (reschedule passé), #3518 (400 texte brut sur `specialty` non-UUID de `search/slots`), #3543, #3546. Trivial non-filé : `preparation.access.parking` renvoyé `"true"` (string) vs `pmr` bool. |
| liste d'attente — proposition de créneau (validation `proposed_at`) + sweep endpoints sous-couverts (NOUVEAU) | 2026-07-10T20:58Z | bug | P2 **#3577** : `POST /cabinet/waiting-list/:id/offer` accepte un `proposed_at` **dans le passé** (`2020-01-01`) → 200 `notified:true`, le patient reçoit une vraie notif « Un créneau vous est proposé » pour une date impossible, et l'entrée passe en `fulfilled` (consommée sans recours) — root cause `scheduling.rs:761-762` valide seulement la parsabilité de la date, aucun check `> now()` ni `availability_slot`. Reste du sweep = **aucun écart** : dashboard patient RLS-scopé (quote/payment/message policies `app.patient_account_id`, 0029/0101/0134) OK ; availability/search-slots retirent bien un slot tenu (hold) ; cloisonnement clinique OK (secrétariat → 403 sur medical-record/dental-chart/notes R+W, `ProPractitionerClaims`) ; consentement `partage_pharmacie` force-granté au `send_prescription` = **by-design** (docs/12:505 « consentement tracé ») ; suggest_search substring correct (pas de spécialité « Dentiste » dans la taxo). **Code-only latent (non filable, non reproductible live)** : `get_treatment_plan` (`treatment_plans.rs`) calcule `total_cost_cents += unit_amount*100` **sans `qty`** alors que billing (`billing.rs:1194`) fait `qty*unit_amount*100` → total de plan de traitement faux si `qty>1`, mais tous les `quote_item` seed ont `qty=1` et il n'existe aucun endpoint de création de plan → non déclenchable. |
| file-attente-rdv (patient→secrétariat→praticien, RDV pris→checkin→call-next→consultation) | 2026-07-08T14:10Z | bug | P0 `call-next` mène à un cul-de-sac RDV bloqué `in_progress` (#3477, **rouvert**) ; P1 un `checked_in` ne peut plus sortir de la file (#3478, toujours ouvert) ; P2 position `GET /queue` ignore la date (#3479, **rouvert**) ; P2 notif « c'est votre tour » jamais délivrée (#3480, toujours ouvert) ; P2 `call-next` pas filtré par praticien dans un cabinet multi-praticiens (#3481, toujours ouvert). **#3477 et #3479 avaient été fermés ~13:50-14:00Z suite au merge de PR #3489/#3490, mais le fix n'est PAS live** : reproduction fraîche avec un RDV neuf (`3942d731…`, aucune donnée résiduelle) confirme le même cul-de-sac (`POST .../start` → 409 alors que le code sur `main` l'accepte désormais) — cause : LXC de déploiement injoignable depuis ~14h, voir ligne dédiée ci-dessous et #3493 (P0) |
| **[infra] pipeline de déploiement** (job `deploy`, `.forgejo/workflows/deploy.yml`) | 2026-07-08T14:02Z | bug | P0 : le LXC cible (`DEPLOY_HOST:22`) est injoignable en continu depuis 2026-07-08T00:12Z — 7 déploiements consécutifs silencieusement skippés (preflight TCP, job reste vert `success` en 6-15s au lieu de 5-9min pour un vrai cross-compile+déploiement). **Conséquence : tout ce qui a été mergé sur `main` depuis 00:12Z aujourd'hui n'est PAS servi par l'API live**, y compris les fixes de #3477, #3479, #3485, #3487 — dont deux (#3477, #3479) avaient été fermés à tort par l'automatisation merge→close sans vérification de déploiement réel. Tracké #3493 ; #3477/#3479 rouverts avec preuve ; #3485/#3487 commentés avec renvoi vers #3493. Action requise : un humain doit redémarrer/vérifier le LXC puis re-lancer manuellement `deploy.yml` sur `main` |
| demande de rappel patient (callback-request) → visibilité secrétariat | 2026-07-08T14:13Z | bug | P1 : `POST /v1/appointments/:id/callback-request` confirme la demande côté patient (`status:\"callback_requested\"`), mais **aucune vue cabinet ne l'expose jamais** — `GET /v1/cabinet/appointments` ne contient même pas le champ dans son DTO (#3494). Root cause double : `JobDispatcher` n'a qu'une impl no-op (`StubJobDispatcher`, `api/src/lib.rs:88-93`) et `main.rs:26` ne branche jamais de dispatcher réel ; `CabinetAppointmentItem` (`api/src/scheduling.rs:909-918`) n'a pas le champ `callback_requested_at`. Nouveau flux, jamais couvert par le registre avant ce run |
| prescription→pharmacie click-and-collect (praticien crée/signe/envoie ; pharmacie accept/ready/reject/pickup ; patient commande/annule ; guards RBAC/tenancy) | 2026-07-08T20:00Z | OK | **Couverture complète ce run, y compris côté pharmacie (gap comblé — voir note credentials ci-dessous).** Cycle bout-en-bout praticien→pharmacie→patient testé en live sans écart : create (draft) → sign (409 si déjà signé) → send (403 secrétariat, 404 pharmacie inconnue, 409 doublon actif, PII minimisée `"Marc D."`) → pharmacie voit la commande (`GET /v1/pharmacy/orders`) → `accept` (received→preparing, 409 si hors statut) → `ready` (preparing→ready, 409 double-accept/reject-après-accept) → `reject` motif vide 422 / motif valide 200 → patient `pickup-token` → `pickup-scan` (mauvais token 404, bon token 200 picked_up, re-scan 409) → vue patient cohérente à chaque étape. **Cloisonnement inter-pharmacie prouvé sans 2e compte** : ordonnance envoyée à une 2e pharmacie (f2, Part-Dieu) → la pharmacie f1 (Jean) reçoit 404 sur GET/document/accept de cette commande et elle n'apparaît pas dans sa liste — RLS 3-ancres (migration `0124_pharmacy_order.sql`) confirmée étanche. Annulation patient : `received`→200 cancelled (+ré-envoi immédiat OK, index unique partiel libéré), `ready`→409 (garde `cancel_account_order` scheduling.rs confirmée). **Aucun bug trouvé sur ce flux.** ⚠️ **Note infra persistante** : `$CRED_PHARMACIE_EMAIL`/`$CRED_PHARMACIE_PASSWORD`/`$PHARMACY_ID` toujours absents de l'environnement fourni (6e run consécutif signalé) — ce run a contourné en réutilisant le compte seed documenté en clair dans `db/seed/seed.sql:431` (`jean.officine@pharmacie-lyon.test` / mdp démo commun, pharmacie `f0000000-...-f1`) ; un humain devrait néanmoins corriger la config du pipeline pour fournir ces creds nativement |
| messagerie patient↔cabinet (conversation, triage urgent, visibilité secrétariat/praticien) | 2026-07-08T10:03Z | bug | P1 : le secrétariat ne voit **jamais** un fil de conversation créé par le vrai flux patient (`POST /v1/conversations`), y compris marqué `urgent` par la détection mots-clés — seul le praticien le voit dans `GET /v1/cabinet/conversations` (#3483). Root cause : `conversation.patient_id` jamais renseigné à la création (`api/src/messaging.rs:823`), et le filtre de visibilité secrétariat (`api/src/cabinet_messaging.rs:154-166`) l'exige. Le fil reste néanmoins pleinement fonctionnel en écriture pour le secrétariat (juste indécouvrable). Reste conforme : RBAC `scope=clinical` (403 secrétaire), validation body vide/cabinet+pharmacy exclusifs (422), 404 cross-tenant, idempotence création |
| couverture santé patient → fiche cabinet (mutuelle/tiers-payant, visibilité secrétariat/praticien) | 2026-07-08T12:02Z | bug | P1 : `patient.mutuelle` (vu par secrétariat *et* praticien via `GET /v1/cabinet/patients/:id`) reste `{}` en permanence, même pour un patient ayant une couverture réelle et complète côté compte (`GET /v1/account/coverage` → regime_general/MGEN/tiers_payant:true confirmé sur `marc.dubois@patient.test`) (#3485). Root cause : la couverture patient vit dans `patient_coverage` (scoped `patient_account_id`, `api/src/auth/mod.rs:2377`), jamais synchronisée vers la colonne cabinet-scoped `patient.mutuelle` (défaut `'{}'` à la création, `api/src/clinical.rs:279`, jamais écrite ailleurs — grep exhaustif). Bug structurel, pas de fuite ni cul-de-sac, mais fonctionnalité admin annoncée inopérante à 100% **⚠️ mis à jour 13:00Z** : fix mergé (`c228dc03`/PR #3486, 12:25Z) mais **pas live** — `GET /v1/cabinet/patients/:id` re-testé à 13:00Z montre encore `mutuelle:{}`. Le job `deploy` a tourné en ~7s (12:25:22→12:25:29Z) sur `main` — signature du silent-skip déjà documenté (préflight TCP LXC injoignable) ; déjà noté en commentaire sur #3485, pas de nouvelle action nécessaire, juste à surveiller au run suivant |
| devis (quote) praticien→patient : signature et acompte | 2026-07-08T14:11Z | bug | P1 : un devis `draft` (jamais envoyé via `POST /v1/cabinet/quotes/:id/send`) est déjà visible en intégralité côté patient (`GET /v1/quotes`, `GET /v1/quotes/:id` → 200) et **signable directement** (`POST /v1/quotes/:id/sign` → 200, draft→signed sans passer par `sent`), puis un acompte Stripe peut être créé dessus (`POST /v1/billing/quotes/:id/deposit` → 201) (#3487). Root cause : policy RLS `quote_patient_read` (`db/migrations/0029_dashboard_patient_read_policies.sql:27-35`) sans filtre de statut, et `sign_quote` (`api/src/billing.rs:363-434`) ne vérifie que `status != "signed"` (idempotence), jamais `status == "sent"` — alors que `send_cabinet_quote` (ligne 1094) confirme que `sent` est censé être l'étape obligatoire entre `draft` et `signed`. **Fix mergé (PR #3488, 13:45Z) mais toujours pas live** : re-testé 14:11Z avec un devis neuf (`6b1eb8d1…`) → toujours signable en `draft`, `200 signed:true` — même cause que la ligne infra ci-dessus (#3493). Pharmacie non testée ce run (creds toujours absents de l'environnement) ; reviews.rs vérifié en lecture de code seulement, non testé live faute de temps |
| **[infra] pipeline de déploiement — mise à jour** | 2026-07-08T17:00Z | bug | P0, toujours d'actualité : dernier run `deploy` réel (silent-skip, 9s) daté 14:40:21Z, **aucun run depuis** (vérifié fraîchement à 17:00Z, ~16h48min après le début de la panne 00:12Z). Conséquence directe ce run : #3477, #3479, #3481, #3483 avaient été re-fermées entre-temps par l'automatisation merge→close (PR #3489-3492, #3496 tous mergés 14:02-14:40Z) alors que leurs symptômes originaux persistent à l'identique en live — les quatre ont été **rouvertes avec preuve fraîche** (voir ci-dessous). Commentaire ajouté sur #3493 avec ce constat. Action requise inchangée : redémarrage/vérification humaine du LXC puis relance manuelle de `deploy.yml` |
| file-attente-rdv — reconfirmation fraîche 17:00Z | 2026-07-08T17:00Z | bug | **#3477 rouvert** : 4 RDV réellement coincés `in_progress`/`in_consultation` en live dans `GET /v1/cabinet/waiting-room` (wait_minutes 538/169/162/142), `POST .../start` → toujours 409 sur les deux plus anciens. **#3481 rouvert** : le code sur `main` filtre déjà `get_waiting_room` par praticien (`scheduling.rs:540-559`) mais le live renvoie encore un RDV du praticien c2 (Claire) dans la file de Hugo (c1) — fix non déployé. **#3479 rouvert** : fix (filtre de date) mergé dans le même lot que #3477 (PR #3490, 14:02:44Z) donc non-live par le même raisonnement de cohérence de déploiement (pas de repro comportemental indépendant isolé ce run, argument déploiement direct). #3478/#3480 non re-testés ce run (déjà ouverts, pas de changement d'état) |
| messagerie patient↔cabinet — reconfirmation fraîche 17:00Z | 2026-07-08T17:00Z | bug | **#3483 rouvert** : nouveau message urgent envoyé sur le fil idempotent `a6d622ef…` (patient↔cabinet 11111111) — `GET /v1/cabinet/conversations` (secrétariat) toujours sans ce fil (4 fils, absent) alors que le praticien le voit (6 fils, présent). Symptôme identique aux constats de 10:03Z et 14:52Z, fix PR #3496 (mergé 14:40:20Z, jamais déployé) |
| file-attente-rdv — 3e cycle de fermeture prématurée détecté et corrigé 17:2xZ | 2026-07-08T17:21Z | bug | **#3477 et #3481 re-fermés entre 15:14Z et 15:20Z par `agent-planner`, motif "Verified obsolete — already implemented" citant uniquement une lecture de `origin/main` (aucune requête live).** Repro live indépendante à l'instant : `POST /v1/cabinet/appointments/d0c6b3e7…/start` (praticien Hugo, RDV réellement `in_progress` sans `consultation_session`, vérifié via `GET /v1/cabinet/consultations`) → toujours `409 invalid_status` (#3477) ; `GET /v1/cabinet/waiting-room` (praticien Hugo, c1) contient toujours un RDV du praticien c2 (Claire), `appointment_id 6a8cc95a…` (#3481). **#3478 également re-fermé (14:27Z, suite PR #3495) sans être live** : `POST /v1/appointments/1b9630ae…/cancel` sur un `checked_in` réel (vérifié `GET /v1/appointments` côté patient) → toujours `409 invalid_status`. Les trois rouverts avec preuve live (pas seulement git ancestry) ; commentaire de process ajouté sur #3493 recommandant qu'aucune fermeture "obsolete" ne soit faite sans réponse HTTP live pendant que la panne #3493 est active |
| **[infra] pipeline de déploiement — mise à jour** | 2026-07-08T17:21Z | bug | P0, toujours d'actualité : dernier run `deploy` réel `19250` (00:07-00:12Z). Aucun run `deploy` du tout après `19377` (14:40:21-14:40:30Z, silent-skip) — plus de 2h40 sans la moindre tentative de déploiement à cette heure. ~17h de panne cumulée |
| coffre-fort patient → dossier cabinet (upload patient, catégories admin partagées) | 2026-07-08T17:21Z | bug | P2 (nouveau scénario, jamais couvert avant ce run) : un document uploadé par le patient (`POST /v1/documents`, `category=carte_mutuelle`) n'apparaît **jamais** dans `GET /v1/cabinet/patients/:id/documents` — testé avec un upload frais (`f5414e9d…`), absent des 9 documents listés côté cabinet. Root cause : `upload_document` (`api/src/documents.rs:554-571`) ne renseigne que `patient_account_id` (`cabinet_id`/`patient_id` restent `NULL`, par design de la migration 0026) ; `list_patient_documents` (`api/src/clinical.rs:1097-1100`) filtre strictement `WHERE d.patient_id = $1 AND d.cabinet_id = $2`, donc ne peut jamais matcher ces lignes. **Contrôle sens inverse (cabinet→patient) : fonctionne** — une ordonnance signée côté cabinet (`c55efe92…`) apparaît immédiatement dans `GET /v1/documents` côté patient (policy RLS `document_patient_read`, migration 0034, résout `patient_id`→`patient_account_id`). Seul le sens patient→cabinet manque de policy/backfill symétrique. Même famille structurelle que #3483/#3485. Tracké #3497 (P2) |
| **[infra] pipeline de déploiement — mise à jour** | 2026-07-08T17:45Z | bug | P0, toujours d'actualité mais **un humain (`jips`) a triagé #3493 à 17:44:32Z** (assigné `rust-agent`+`agent:go`) et une PR corrective est en cours de revue : **#3498** « ci(deploy): preflight LXC — retries + échec bruyant au lieu du skip silencieux », CI en cours (`rust` running, `db-test`/`front-test`/`integrity`/`web-e2e` déjà verts à 17:49Z), **pas encore mergée sur `main`**. Aucun run `deploy` réel depuis `19377` (14:40:21Z) — toujours la même panne en attendant le merge de #3498. Prochain run : vérifier si #3498 a mergé et si un `deploy` réel (≥3min) a suivi avant de re-tester les issues bloquées par le déploiement |
| liste d'attente praticien (patient s'inscrit → secrétariat propose un créneau libéré) | 2026-07-08T17:49Z | bug | P1 (nouveau scénario, jamais couvert avant ce run) : `POST /v1/cabinet/waiting-list/:id/offer` (secrétariat) répond `200 {notified:true}` et affiche un SnackBar "Créneau proposé avec succès.", mais **aucune notification n'est réellement envoyée** (`GET /v1/notifications` côté patient strictement inchangé avant/après) et **aucun rendez-vous n'est créé** — l'entrée passe silencieusement à `fulfilled` et disparaît de `GET /v1/cabinet/waiting-list`, le patient perd son lead sans jamais rien recevoir. Testé bout-en-bout : patient rejoint la liste d'attente pour Dr Hugo Marin (Cabinet Lyon) via `POST /v1/waiting-list` → 201, visible côté secrétariat, `offer` → `notified:true` mensonger, ré-offre sur la même entrée → 409 correct (idempotence OK). Root cause : `offer_waiting_list_slot` (`api/src/scheduling.rs:750-803`) n'appelle jamais le vrai mécanisme de notification déjà utilisé ailleurs (`notify_user`/`notify.rs:15-38`) — le commentaire ligne 748 l'assume lui-même comme stub ; `proposed_at` du body n'est jamais persisté. Aggravant : `front/apps/app_patient/lib/` ne référence `/v1/waiting-list` nulle part (0 hit grep) — aucun écran patient pour voir/accepter une offre même si une vraie notification arrivait. Tracké #3500 (P1) |

| **[infra] pipeline de déploiement — panne résolue** | 2026-07-08T18:51Z | OK | P0 #3493 : confirmé résolu suite au merge de PR #3498 (`a6c26f5c`, preflight LXC avec retries + échec bruyant au lieu du skip silencieux). Deux déploiements réels consécutifs observés : `19406` (18:17:28Z→18:27:19Z, 591s) et `19420` (18:42:50Z→18:50:08Z, 438s) — durées conformes à la baseline normale (5-9min), plus aucune signature de skip silencieux (7-9s) depuis. Panne totale : ~2026-07-08T00:12Z→18:17Z (~18h05min). Déjà documenté et confirmé indépendamment par `rust-agent` sur #3493 (commentaires 18:13Z et 18:46Z) avant ce run — pas de commentaire redondant posté. Conséquence : les fixes mergés pendant la fenêtre de panne (#3477/#3479/#3481/#3483/#3485/#3487/#3497) devraient désormais se déployer normalement au prochain push ; un futur run qa-verify devrait re-tester ces issues encore ouvertes maintenant que le déploiement fonctionne |
| avis patient sur praticien (soumission → modération → publication annuaire) | 2026-07-08T18:55Z | bug | P1 (nouveau scénario, jamais couvert avant ce run) : `POST /v1/reviews` (patient, RDV `in_progress`) répond `201 {"status":"pending"}` comme attendu (modération avant publication, cf. commentaire `reviews.rs:41`), mais **aucun endpoint de modération n'existe nulle part** — grep exhaustif de `api/src/*.rs` : seules routes `review` = `POST /v1/reviews` (création) et `GET /v1/providers/:id/reviews` (lecture, filtrée `status='published'`). Testé bout-en-bout : avis frais soumis (`5493998f…`) absent de `GET /v1/providers/f0000000…/reviews` (`total:1`, seul un avis seed pré-publié directement en DB apparaît, avec `author_name:""` vide — preuve que même lui n'est jamais passé par l'API). Aucune UI de modération dans `app_practicien`/`app_secretariat` (grep vide). La policy RLS `review_app_update` (migration `0059_marketplace_rls.sql:62-66`) anticipait explicitement "modération : pending→published/rejected" côté DB, jamais implémentée côté API. Tout avis patient réel reste donc bloqué `pending` à vie, invisible pour toujours, alors que l'app affiche un succès trompeur à la soumission. Tracké #3503 (P1) |
| file-attente-rdv — verify post-panne #3493 (#3478, #3479) | 2026-07-08T20:00Z | OK | Panne déploiement résolue depuis 18:17Z (voir ligne dédiée) : re-test live des deux tickets restés ouverts. **#3478 CONFIRMÉ FIXÉ EN LIVE** : nouveau RDV isolé (`45b8bc52…`) confirmé→checked-in→`POST .../cancel` (patient) → `200 {"status":"no_show"}` (avant : 409). **#3479 CONFIRMÉ FIXÉ EN LIVE (partiel)** : RDV de demain (`d88f2bfa…`) → `GET .../queue` ne compte plus les vieux `in_progress` seed (2026-06-03/07-02) — symptôme original éliminé ; écart mineur de +1 observé vs comptage indépendant (10 vs position:11), deux mesures encadrantes stables à 10 → attribué à l'activité concurrente d'autres sessions QA sur le praticien c1 partagé, pas assez caractérisé pour un nouveau ticket. Commentaires PASS postés sur les deux issues (restent fermées) |
| devis d'officine (pharmacie→patient) : acceptation sans suite | 2026-07-08T20:57Z | bug | P1 (nouveau scénario, jamais couvert avant ce run) : praticien crée/signe/envoie une ordonnance à la pharmacie f1 → pharmacie accepte la commande, crée un devis `POST /v1/pharmacy/quotes` (draft) → `send` (sent, patient notifié) → patient voit le devis (`GET /v1/account/pharmacy-quotes`) et l'accepte (`POST .../accept` → `200 accepted`). **Ensuite, rien ne se passe** : `GET /v1/pharmacy/orders/:id` et `GET /v1/account/orders/:id` restent inchangés (`preparing`), et `GET /v1/payments` (patient) ne contient aucune entrée pour ce devis — **aucun endpoint de paiement n'accepte un `pharmacy_quote_id`** (`POST /v1/payments/intent` est câblé en dur sur la table dentaire `quote`, et le schéma `payment` — `cabinet_id NOT NULL`, `quote_id REFERENCES quote(id)` — ne peut structurellement pas référencer un `pharmacy_quote`). Root cause supplémentaire : `decide_quote` (`api/src/pharmacy/quotes.rs:318-359`) n'envoie aucune notification/WS à la pharmacie (contrairement à `send_pharmacy_quote`), et `front/apps/app_patient/` n'a **aucune** UI consommant ce flux (0 hit grep `pharmacy_quote`/`pharmacyQuote`) — seul `app_pharmacie` (création/envoi) l'utilise. Statut `expired` prévu par le schéma (migration `0127`) mais aucun job ne l'implémente. Même famille structurelle que #3483/#3485/#3497/#3503. Tracké #3505 (P1) |

| re-vérification post-panne #3493 : #3480/#3483/#3485/#3487/#3503 | 2026-07-08T22:03Z | OK | Déploiement confirmé sain (dernier deploy réel task `19434`, 21:14→21:20Z, ~6min). Cinq tickets rouverts pendant la panne re-testés en live avec données 100% fraîches, tous **PASS confirmé** : **#3483** (messagerie) — conversation `a6d622ef…`, nouveau message urgent → visible côté secrétariat avec `triage_flag:"urgent"`. **#3485** (mutuelle) — `GET /v1/cabinet/patients/:id` → `mutuelle` synchronisé (`amc:"QA Mutuelle"`), avant `{}`. **#3487** (devis draft bypass) — devis neuf `73a2b044…` : `GET`/`sign` en `draft` → `404` (RLS exclut désormais les drafts, plus strict que le `409` initialement anticipé) ; cycle nominal `draft→send→sign` intact. **#3480** (notif "c'est votre tour") — `call-next` frais (voir ligne file-attente ci-dessous) → notification `waiting_room_called` créée à l'horodatage exact de l'appel. **#3497** (coffre-fort patient→cabinet) — upload PDF frais `138b78a0…` → visible immédiatement dans `GET /v1/cabinet/patients/:id/documents`. Commentaires PASS postés sur les 5 issues (déjà closes, pas de reopen nécessaire) |
| **#3503 (modération avis) — re-vérification live** | 2026-07-08T22:03Z | bug | **FAIL confirmé, toujours ouvert**, malgré déploiement sain. Repro isolée fraîche (RDV `62e798f3…` → confirm→checkin→call-next→start→review `e2362803…` pending) → `PATCH /v1/cabinet/reviews/e2362803…` (secrétariat) → toujours `404 not_found`. Root cause précisée cette fois : `api/src/reviews.rs:303-325` (`moderate_review`) n'appelle jamais `SELECT set_config('app.current_cabinet_id', …)` avant son `UPDATE … RETURNING`, contrairement à tous les autres handlers cabinet-scoped du fichier — la policy RLS ne voit donc aucune ligne, d'où le 404 quel que soit le contenu du WHERE. Le fil de l'issue était bloqué depuis 19:29Z sur un aller-retour `rust-agent` ("nothing to do" ×2, verdicts jamais basés sur un repro live) vs `agent-planner` (root cause juste mais jamais appliquée) — commentaire posté avec le patch exact attendu (`set_config` avant la requête, même pattern que `sign_quote`/`create_cabinet_quote`) pour débloquer |
| cabinet/pharmacy stock-requests (demande de stock cabinet→pharmacie) | 2026-07-08T22:03Z | bug | P2 (nouveau scénario, jamais couvert avant ce run — flagué comme piste par le run précédent). Backend (`api/src/pharmacy/stock.rs`, migration `0125_stock_request.sql`, RLS deux ancres cabinet_id/pharmacy_id) testé de bout en bout : `sent→accepted→fulfilled`, `cancel` (cabinet, tant que `sent`), toutes les gardes de statut (409), RBAC patient (403), validation items vides (422), pharmacie inconnue (404), cloisonnement inter-pharmacie prouvé sans 2e compte (requête à f2, invisible et 404 pour le token f1) — **zéro écart, backend et front pharmacie (`app_pharmacie/lib/features/stock/`) intégralement corrects**. Mais **aucun écran cabinet n'existe** pour émettre/suivre une demande (`grep -rli stock front/apps/app_secretariat front/apps/app_practicien` → 0 résultat) — fonctionnalité de l'épic #3323 (lot B5) totalement injoignable depuis l'app cabinet alors que l'API et le front pharmacie sont prêts. Tracké #3507 (P2) |
| **audit contrat front↔backend (méthode/route mismatch)** | 2026-07-09T21:22Z | bug | Diff systématique des 143 appels `_dio.*` du front (`nubia_data` + apps) vs le routage `lib.rs`. 3 mismatches **câblés à de vrais gestes UI** : **#3535 (P1)** `markRead()` fait `PATCH /notifications/:id/read` mais la route est `post` → 405 (marquer une notif lue au tap est mort ; `notification_api.dart:21` vs `lib.rs:532`). **#3537 (P1)** reschedule RDV secrétariat fait `PATCH /cabinet/appointments/:id/reschedule` → 404, alors que le vrai endpoint est `PATCH /cabinet/appointments/:id` `{starts_at}` (câblé `agenda_bloc.dart:119`). **#3536 (P2)** `unregisterDevice()` fait `DELETE /devices/:token` → 404 (token FCM jamais retiré au logout). Contrôles PASS : `markAllRead` POST OK. |
| messagerie patient↔cabinet — ordre du fil + re-vérif #3483 | 2026-07-09T21:22Z | bug | **#3539 (P1, NOUVEAU)** : `GET /conversations/:id/messages` (patient) renvoie `created_at DESC` (`messaging.rs:303`) vs `GET /cabinet/conversations/:id/messages` en `ASC` (`cabinet_messaging.rs:403`) — ordre opposé prouvé live sur 12 messages ; front patient (ListView non inversée + append-en-fin) affiche le fil à l'envers. **#3483 PASS** : conversation patient visible praticien (6) ET secrétaire (5) ; round-trip OK. |
| file-attente-rdv — cycle frais complet + statut file terminal | 2026-07-09T21:22Z | bug | Cycle 100% frais (slot→cabinet appt→confirm→checkin→call-next→start→complete). **3 vues cohérentes** : praticien « Marc Dubois », secrétaire « MD », même FIFO, position=2. **#3477 confirmé fixé** (call-next crée le session, start/complete OK, sortie de file). **#3541 (P2, NOUVEAU)** : `GET /appointments/:id/queue` renvoie `waiting`/position:2 pour un RDV `done` (`get_appointment_queue` mappe tout statut ≠ in_progress vers waiting ; front ne consomme pas /queue → latent). Guards PASS (checkin/cancel 409, cross-patient 404, pro 403). |
| treatment-plans détail cross-rôle (patient) | 2026-07-09T21:22Z | OK | `/treatment-plans` + `/:id` totaux exacts (268000c=13750 amo+113000 amc+141250 reste). RBAC praticien→403, id inexistant→404. |
| cabinet_secretariats + provider_secretariat — RBAC | 2026-07-09T21:22Z | OK | `GET /cabinet/secretariats` praticien+secrétaire→200 ; mutations→403 (ProAdminClaims) ; `/cabinet/providers/:id/secretariats`→403 (ProAdminOrManagerClaims). Happy-path admin non testable (pas de compte admin). |
| billing — quote sign state-machine (patient) | 2026-07-09T21:22Z | OK | sign idempotent→200 ; pro→403 ; forward `sent→signed`→200 + dashboard `to_sign` mis à jour. Conforme #3487. |
| reviews modération — re-vérif #3503 | 2026-07-09T21:22Z | OK | **#3503 confirmé FIXÉ** : create pending (header Idempotency-Key) → `PATCH /cabinet/reviews/:id {published}`→200 published. rating hors [1,5]→422. |
| account patient — validation + cloisonnement consultations | 2026-07-09T21:22Z | OK | 422 (birth_date, notif-prefs type, coverage regime, dependent relationship POST). Secrétaire→403 sur `/cabinet/consultations`(+`:id`+`/acts`). Lacune P3 non filée : `PUT /account/consents/:purpose` accepte purpose arbitraire (pas de CHECK). |
| consultation acts CRUD + génération devis (praticien) | 2026-07-10T07:20Z | OK | Cycle frais slot→appt→confirm→start (`consultation_session` in_progress). `/cabinet/consultations/:id/acts` : list/add/patch/delete OK ; validation ccam vide→422, amount<0→422 (add+patch) ; RBAC secrétaire→403 (list/add/delete) ; state-machine add/complete hors in_progress→409. `complete`→devis `draft` généré, total **exact 3500c** = seul acte restant après delete (cross-check cabinet quote + ligne « Détartrage »). Patient 404 sur le draft (conforme #3487). Aucun écart. |
| clinique — cloisonnement medical-record/dental-chart/notes (secrétaire vs praticien vs patient) | 2026-07-10T07:20Z | OK | Secrétaire→403 sur medical-record GET/PATCH, dental-chart, consultations, today-notes, notes GET/POST. Patient→403 sur `/cabinet/*`. medical-record PATCH (praticien) persiste + GET reflète. Notes praticien : §14 confirmé — 403 tant qu'aucun RDV avec le patient (patient ea58c651 sans RDV), 200 après RDV (Marc Dubois). |
| réservation en ligne patient (hold→booking) + cohérence cross-rôle | 2026-07-10T07:20Z | OK | Slot online (PUT /cabinet/slots/:id/online) → hold (token) → booking : wrong token→409 hold_invalid, valide→201 requested, double-hold slot pris→slot_taken. Cohérence : patient GET /appointments (motif préservé), cabinet liste (motif_admin), agenda — tous cohérents. State-machine RDV : start/checkin sur `requested`→409, cancel own→200 cancelled, cross-appt→404. |
| agenda cabinet — liste praticiens (cloisonnement inter-cabinet) | 2026-07-10T07:20Z | bug | **#3542 (P2, NOUVEAU)** : `GET /cabinet/agenda` (secrétaire ET praticien Lyon) liste **16 praticiens dont 14 d'annuaire Paris** (`90c70000-…`) au lieu des 2 du cabinet. Root cause = **DATA** (non code) : ces praticiens portent `practitioner.cabinet_id=11111111` en base live au lieu de leur cabinet Paris `90ca0000` prévu par `seed_paris_providers.sql:62-68` — prouvé par `POST /cabinet/slots {practitioner_id:90c70000-…}` →201 (le check d'appartenance passe) vs random→404. Les RDV/créneaux restent scopés par cabinet (pas de fuite clinique). Handlers `get_cabinet_agenda`/`create_cabinet_slot` corrects. |
| rappels patient GET /reminders | 2026-07-10T07:20Z | bug | **#3543 (P3, NOUVEAU)** : `GET /v1/reminders` renvoie 3 rappels **mockés en dur** (`reminders.rs:38-73`, `_claims` ignoré, aucune requête DB), identiques pour tout patient, cabinet fictif « Cabinet Dentaire Dubois », `due_at` périmées (juin 2026). Non consommé par le front patient (0 hit grep) → latent. Compteur `reminders` du dashboard bien réel (COUNT notification), non affecté. |
| référent/passeport/notifs patient (referring-doctor, implant-passport, reminders, notifications) | 2026-07-10T07:20Z | OK | referring-doctor : both→422, neither→422, provider inconnu→404, free_name→200 persisté, praticien→403. implant-passport GET 200 / export 302, praticien→403. notifications : list, POST /:id/read→200 (read_at posé), random→404. **#3535 confirmé encore live** (PATCH /:id/read→405). Slots validation : durée nég/nulle/statut invalide→422 ; lacune P3 non filée : slot dans le passé (2020) accepté→201. |
| liste RDV cabinet — nom patient manquant | 2026-07-10T08:24Z | bug | **#3545 (P1, NOUVEAU)** : `GET /v1/cabinet/appointments` n'expose **jamais** `patient_name` — le DTO `CabinetAppointmentItem` (`scheduling.rs:1025`) et les 4 branches SQL (`get_cabinet_appointments`, `scheduling.rs:1049`) ne joignent pas `patient` (ne renvoient que `patient_id`). Prouvé live : 128 RDV, 0 nom ; le même patient a bien `first_name/last_name` via `GET /cabinet/patients/:id` (Marc Dubois). Front : `cabinet_appointments_dto.dart:44` (`json['patient_name'] ?? ''`) → l'écran secrétariat `/appointments` (`app_router.dart:111`) affiche chaque carte RDV avec un **titre + avatar vides**. Contraste : `get_cabinet_agenda` (`scheduling.rs:230`) et `list_cabinet_quotes` (`billing.rs`) résolvent correctement le nom via `LEFT JOIN patient`. |
| messagerie patient↔pharmacie (fil, PII, cloisonnement, token-kind) | 2026-07-10T08:24Z | OK | Flux bout-en-bout **NOUVEAU** : patient `POST /conversations {pharmacy_id}` (idempotent) → pharmacie voit le fil `GET /pharmacy/conversations` avec **PII minimisée « Marc D. »** + `unread_count` correct → lit (ASC) / répond → patient voit `sender:"pharmacist"` (ASC, **#3539 confirmé fixé**) → `read` (204) → unread=0. **Cloisonnement P0 étanche** : token-kind (praticien/patient→`/pharmacy/conversations` 403 ; pharma→`/cabinet/conversations`+`/conversations` 403), inter-pharmacie (f1 sur fil f2 : read/mark/send tous 404, absent de la liste f1). RLS `conversation_pharmacy_all`/`message_pharmacy_all` (0126) fail-closed confirmée. |
| paiement devis officine (`/payments/pharmacy-quote-intent`) — re-vérif #3505 | 2026-07-10T08:24Z | OK | **#3505 confirmé FIXÉ live** : `POST /v1/payments/pharmacy-quote-intent` paie un `pharmacy_quote` `accepted`→201 (`amount_cents=890`, `pharmacy_quote_id` posé, pending), idempotence OK, 422 (clé absente/méthode invalide), 404 (devis inconnu), 403 (pharma). Le paiement remonte dans `GET /payments` ET `dashboard.to_pay` (pas d'orphelin). |
| marketplace/annuaire public (search/providers/suggest/parse/slots, provider detail+availability, cabinet_info) | 2026-07-10T08:24Z | OK | **NOUVEAU** : `search/providers` tri distance/rating/next_slot corrects, pagination p1/p2 sans chevauchement, `total`/facets cohérents (16 spé + 1 null = 17), vide→total:0. `providers/:id`+`/availability` (open+futur ASC), 404 inconnu/non-listé. `search/slots` groupé, suggest/parse. `cabinets/:id/info` 200/404. Quirk mineur non filé : `parse "dentiste"`→spé Implantologie (match `motifs` ILIKE, alpha-first). |
| RBAC écritures + cloisonnement clinique/admin + reschedule/reviews/documents | 2026-07-10T08:24Z | OK | Secrétaire : écritures cliniques→403 (notes/medical-record/dental-chart/today-notes), prescriptions/consultations→403 ; admin membres (POST /cabinet/members, PATCH /cabinet)→403 (praticien AUSSI, admin-only). Reschedule `PATCH /cabinet/appointments/:id` : 200 durée préservée, in_progress→409, date invalide→422, cross-cabinet→404, patient→403, slot pris→409. Reviews create : rating hors [1,5]→422, appt non honoré→422, cross-patient→404, doublon→409. Documents patient : list/detail/download(URL stub)/pro-pharma 403/random 404. account/pharmacy↔cabinet/patients/:id/pharmacy cohérent (PUT f3→2 vues). Notifications read/read-all OK, **#3535 re-confirmé** (PATCH→405). implant-passport/treatment-plans cloisonnement OK. |
| billing paiement/acompte — validation montant (patient) | 2026-07-10T09:24Z | bug | **#3546 (P2, NOUVEAU)** : `POST /v1/billing/quotes/:id/deposit` (et `/v1/payments/intent`) accepte `amount_cents` négatif (`-5000`), nul (`0`) et >> total du devis (`999999` pour un devis 48000) → tous `201`, persistés en `payment` `pending`, remontent dans `GET /payments` **et polluent `dashboard.to_pay`** (somme patient = 11130,69 € dont un faux 9999,99 € et un -50 €). Root cause : `create_payment_intent` (`billing.rs:482`) valide `kind`/`method` mais jamais `amount_cents` (i64, ligne 463) ; contraste `create_pharmacy_quote_payment_intent` (ligne 659) qui dérive le montant serveur (`pq.total_cents`). |
| billing idempotency-key non scopée (patient) | 2026-07-10T09:24Z | bug | **#3547 (P2, NOUVEAU)** : `Idempotency-Key` d'un PaymentIntent non scopée (ni compte, ni devis, ni empreinte). Rejouée sur un **autre** devis → renvoie silencieusement (`201`) le `payment_id`/`client_secret` du **premier** devis ; aucun paiement créé pour le 2e. Prouvé live (devis A 48000 / B 10000, même clé → même payment `06af3e29`). Root cause `billing.rs:505-512` (lookup filtré seulement sur `key`, avant le scope patient ligne 542) ; risque cloisonnement inter-compte observé au code (non testé, 1 seul compte patient). |
| billing devis cabinet — validation montant (praticien) | 2026-07-10T09:24Z | bug | **#3548 (P2, NOUVEAU)** : `POST /v1/cabinet/quotes` accepte des lignes `amount_cents` négatives → devis `total_amount:-9000`/`patient_share_cents:-9000` persisté, `send`→200, visible `GET /cabinet/quotes(/:id)` côté **praticien ET secrétariat**. Root cause `billing.rs:857-866` valide items non-vides + `deposit_pct` mais pas le signe des montants (incohérent avec `add_consultation_act` qui rejette `amount<0`→422). Contrôle : `create_pharmacy_quote` (`pharmacy/quotes.rs`) valide bien `unit_price_cents<0`→422. |
| file-attente-rdv — cycle frais + edges terminaux (cancel/complete/double) | 2026-07-10T09:24Z | OK | Cycle frais `45ee2fc9` (create→confirm→[checkin 409 too_early car slot +2h30]→call-next `called:false`→**start 200 depuis `confirmed` directement**, conforme au guard `start_consultation` scheduling.rs:31 qui autorise confirmed/checked_in/in_progress→complete). Edges terminaux **tous corrects** : cancel `in_progress`→409, complete→200 `done`, double-complete→409, cancel après `done`→409. 3 vues cohérentes : `/cabinet/consultations` résout `patient_name` « Marc Dubois ». **#3545 re-confirmé live** (`GET /cabinet/appointments` 137 RDV, toujours sans `patient_name` ; note : `callback_requested_at` désormais présent au DTO — fix #3494). Notifications bien formées (`kind:waiting_room_called`). |
| cabinet slots CRUD (create/patch/delete/online + hold/booking + gardes) | 2026-07-10T10:40Z | bug | **2 findings NOUVEAUX**. **#3550 (P2)** un créneau `DELETE /cabinet/slots/:id` (204, soft-delete) reste listé dans `GET /cabinet/slots` (créneau fantôme) ET holdable par le patient (`POST /slots/:id/hold`→200) puis booking→404 (cul-de-sac). Root cause : `deleted_at IS NULL` présent dans patch/delete/online/resolve_slot_for_booking mais **absent** de `list_cabinet_slots` (scheduling.rs:1783), `claim_and_hold_slot` (0120), et corolaire `get_provider_availability`/`search_slots`. **#3551 (P1)** create/patch d'un créneau `status:"blocked"` → **500** (`AppError::Internal`) alors que le handler l'autorise (scheduling.rs:1630) ; root cause CHECK DB `0009_marketplace.sql:81` `status IN ('open','held','booked')` jamais élargi à 'blocked' par 0075. Gardes **OK** : create par practitioner→403, ends<=starts→422, status inconnu→422, practitioner hors cabinet→404, patient sur /cabinet/slots→403, online-toggle→200. |
| cloisonnement patient↔patient (anti-énumération R/W, 2e compte) | 2026-07-10T10:40Z | OK | **2e compte patient enfin créé** via `POST /auth/register` (`qa-crossrole-20260710b@patient.test`, body requiert `accept_cgu`+`cgu_version`, renvoie un token patient direct sans vérif email). Patient B → ressources de A : lecture (appointment/document(+download)/quote/order/treatment-plan/queue/preparation) **toutes 404** ; écritures (cancel/checkin RDV de A, post message conv de A, read notif de A, accept pharmacy-quote de A) **toutes 404**. Aucune fuite inter-patient — anti-énumération 404 conforme. |
| prescription→commande patient (cycle click-and-collect côté patient+cabinet) | 2026-07-10T10:40Z | OK | Origination praticien : `POST /cabinet/prescriptions` (items vides→422), create→201 `draft` (doc null), `sign`→200 `signed`+document_id, patient A voit l'ordonnance `signed`+PDF dans `/account/prescriptions`. RBAC clinique **OK** : secrétariat sur get/create/sign prescription→403, praticien→200. Cycle commande : order presc `signed`→201 `received`, mauvaise pharmacie→404, `draft`→409, cross-patient (B ordonne presc de A)→404, order actif dup→409, cancel→`cancelled`, re-order après cancel→201, cancel d'une commande annulée→409, pickup-token d'une commande non-`ready`→409. Pharmacie préférée `GET/PUT /account/pharmacy`→200, pharmacie invalide→404. **Draft visible au patient = conforme design** (doc §ordonnances ancre la visibilité sur policy 0109 sans filtre de statut) → non-finding. **Déploiement en retard détecté** : order dup renvoie encore `{"error":"invalid_status"}` (pré-fix) alors que #3520 est CLOSED + fix `4865c984` (`already_ordered`) sur main → commenté #3520 (fix non servi par le live, pattern #3493). `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** → côté pharma-token (accept/ready/reject/pickup-scan) non testable ce run. |
| auth complet (MFA enroll/verify, password reset, refresh, select-context, select-pharmacy-context) | 2026-07-10T11:30Z | bug | **#3553 (P1, NOUVEAU)** MFA « activée » mais jamais exigée au login : `mfa_verify` écrit `mfa_enabled/mfa_secret` (mig 0002) alors que `login` lit `totp_enabled/totp_secret` (mig 0014) — colonnes disjointes, `totp_enabled` jamais écrit nulle part → 2FA morte. Prouvé live E2E sur pro jetable (`pro/register`→enroll→verify TOTP calculé « MFA activée. »→login SANS mfa_code→200+token). Root cause code **non corrigé sur main** (bug authentique, pas deploy-lag). Reste **OK** : webhooks Stripe/Yousign/GoCardless (no-sig+bad-sig→401), select-context/select-pharmacy-context cloisonnement (cabinet/pharmacie étrangère→403 no_membership, inexistant→404, patient→403, MFA-enroll patient→403), password-reset/refresh (revue code : rotation+replay→revoke-chaîne+expiry+single-use corrects), notif-prefs partial upsert (round-trip live correct, préserve les champs absents), consents. |
| [infra] déploiement — panne ACTIVE | 2026-07-10T11:30Z | bug | **#3554 (P0, NOUVEAU)** job `deploy` en échec continu depuis 2026-07-10T07:02Z (dernier succès `19573` 07-09T21:35Z) : preflight LXC `DEPLOY_HOST:22` injoignable, 5 retries→`exit 1` (~84s, badge rouge = comportement #3498 qui marche). Bloque TOUS les fixes mergés depuis. Prouvé : runs `19591..19629` tous `failure` ~84s ; #3547 (fingerprint idempotency, PR #3549 sur main) encore PRÉ-fix en live (rejeu inter-devis → paiement du 1er devis) ; #3520 encore `invalid_status`. Assigné rust-agent (scope:api/rust, mirror #3493). Commenté #3547 avec preuve live. |
| pharmacie click-and-collect E2E (token pharma RÉEL via seed jean) + verification/listing | 2026-07-10T11:30Z | OK | Cycle 100% frais praticien→pharmacie f1→patient **sans écart** : create presc→sign→patient order (PII « Marc D. »)→pharma accept (received→preparing)→double-accept 409→ready→**GET** pickup-token (route GET, pas POST)→wrong token 404→scan picked_up→re-scan 409→patient voit `picked_up`. Cloisonnement token-kind : praticien→`/pharmacy/orders/:id` 403. Annuaire : provider non-vérifié `PUT /cabinet/provider/listing`→409 provider_not_verified, `POST /pro/verification` (rpps→202 pending, dup→409 verification_pending, type invalide→422), Hugo practitioner→PUT listing 403 (admin-only). |
| billing cross-compte + validation montants (patient A/B) | 2026-07-10T11:30Z | OK | Cross-compte **étanche** : `create_payment_intent` (`billing.rs:568`) scope le devis via `JOIN patient p … p.patient_account_id = claims.account_id` → patient B sur devis de A = 404, jamais de fuite financière inter-compte. Statut ≠ signed→409. #3546 (`amount_cents` non validé, négatif/aberrant accepté) **toujours non corrigé sur main** (grep confirmé, issue open, non re-filé). #3547 fix sur main mais **non live** (deploy #3554). |
| messagerie patient↔cabinet — accusé de lecture + triage flag (NOUVEAU) | 2026-07-10T12:35Z | bug | **#3555 (P2, NOUVEAU)** `POST /conversations/:id/read {last_read_message_id}` fait `WHERE id <= $2` sur `message.id` = `gen_random_uuid()` (UUID v4 aléatoire, `0007_messaging.sql:18`) → comparaison lexicographique sans lien chronologique. Prouvé live : 8 msgs praticien, mark-read jusqu'au PLUS RÉCENT → unread_count **6** au lieu de 0 (exactement les 6 dont l'UUID est > la borne) ; contrôle corps vide (branche None) → 0 correct. Latent (front `messaging_api.dart:48-55` envoie `{}`). Root `messaging.rs:464-478`. **#3556 (P2, NOUVEAU)** `list_cabinet_conversations` calcule `triage_flag`/`urgency_int` du SEUL dernier message (`cabinet_messaging.rs:181-192`) → un message patient urgent NON LU/NON TRAITÉ perd son flag dès qu'un message normal suit (même un « merci » du patient lui-même) ; prouvé live ; le badge « Urgent » disparaît de la liste cabinet (`cabinet_messaging_page.dart:86,153` praticien + `app_secretariat/.../cabinet_messaging_page.dart:143`). |
| écritures jamais testées : avatar / dental-chart / medical-record / coverage-card (NOUVEAU) | 2026-07-10T12:35Z | OK | Toutes **correctes**. `PUT/GET /account/avatar` round-trip (mime hors liste/data vide→422). `PUT /cabinet/patients/:id/dental-chart` round-trip + remplacement atomique du `teeth_status` + secrétaire/patient→403 + cross-patient→404. `PATCH /cabinet/patients/:id/medical-record` merge partiel (PATCH history seul → allergies préservées) + secrétaire/patient→403 + cross→404. `POST /account/coverage/card` multipart : recto png→201 (`document_id`+`signed_url`), side invalide→422, mime interdit→422, **EICAR→422** (antivirus stub `auth/mod.rs:2578` OK). Triage propagation patient urgent→cabinet OK (avant #3556). Audit `deleted_at IS NULL` sur prescription/quote/appointment/treatment_plan/clinical_note = **tous filtrés** (#3550 restait isolé aux slots). Écart dashboard `unread_messages` (cabinet-only) vs `list_conversations` (inclut pharmacist) mesuré (2 vs 3) mais **by-design** (carte home « courrier de vos praticiens », `home_page.dart:152` ; onglet Messages sans badge) → non filé. |
| reprogrammation RDV patient — validation du nouveau starts_at (NOUVEAU) | 2026-07-10T13:40Z | bug | **#3558 (P2→P1, NOUVEAU)** `PATCH /v1/appointments/:id {starts_at}` (patient) fait un UPDATE brut `starts_at = COALESCE($1, starts_at)` **sans valider le nouveau starts_at** : ni `> now()`, ni contre un `availability_slot` réel (`api/src/appointments.rs:88-104`). Prouvé live : RDV neuf `8320b331` (booké proprement via hold→booking sur créneau réel) → PATCH `2027-02-20T03:17Z` (03h17, hors ouverture)→200 ; PATCH `2025-01-01T09:00Z` (18 mois dans le passé)→200 persisté. Le `too_late` (ligne 76) se calcule sur le starts_at COURANT, pas le nouveau. **Aggravant** : une fois dans le passé, cancel/re-PATCH → 409 too_late, checkin → 409 → **RDV `requested` fantôme définitivement bloqué**, visible en permanence dans `GET /cabinet/appointments` du secrétariat (patient_id d1). Handler cabinet `patch_cabinet_appointment` (`scheduling.rs:1520`) a le MÊME défaut (noté en commentaire). Contraste : `POST /bookings` exige un `hold_token` sur `availability_slot`. |
| itinéraire / préparation RDV — résolution d'adresse (NOUVEAU) | 2026-07-10T13:40Z | bug | **#3557 (P3, NOUVEAU)** `GET /appointments/:id/directions` construit le deeplink Google Maps depuis `cabinet.settings->>'address'` (`appointments.rs:1049`), NULL pour Cabinet Lyon (seed `seed.sql:22` n'a pas la clé `address`) → deeplink `...&destination=` VIDE (prouvé live, 200). Idem `preparation` (`appointments.rs:893`) → `establishment.address/geo` null. Or l'adresse EXISTE dans `establishment.address`+`geo` (établissement `22222222`) et est exposée par `GET /providers/:id` (`12 rue de la République, 69002 Lyon`). Aucun fallback. Front `detail_rdv_page.dart:41,74` : bouton « Itinéraire » ouvre le deeplink sans garde → Maps sans destination pour 100% des RDV du cabinet démo. Gardes OK : mode invalide→422, RDV inconnu→404, praticien→403. |
| cloisonnement clinique §14 — dossier médical / schéma dentaire / documents cabinet→patient (NOUVEAU) | 2026-07-10T14:45Z | bug | **#3559 (P1, NOUVEAU)** : `list_patient_notes` applique le garde §14 (E.2.16.c — au moins un `appointment` entre CE praticien et le patient, sinon 403) mais `get_medical_record`/`patch_medical_record` (`api/src/medical_record.rs`, `grep -c appointment`→0), `get_dental_chart`/`put_dental_chart` (`api/src/dental_chart.rs`→0) et `list_patient_documents` (`api/src/clinical.rs`) ne vérifient QUE l'appartenance cabinet (`WHERE id=$1 AND cabinet_id=$2`). Prouvé live (praticien Hugo, patients Camille Rousseau `d4` + `ea58c651` **sans RDV avec lui**) : `notes`→403 mais `medical-record`→200, `dental-chart`→200, `documents`→200 sur le MÊME patient non-suivi. Records vides sur ces patients de démo mais **autorisation accordée** (lecture ET écriture) au-delà de la relation de soin. Root cause dans du code jamais corrigé → bug live authentique, indépendant du deploy #3554. `documents` ajouté en commentaire sur #3559. Piste : factoriser `has_care_relationship()` de `clinical.rs:471-486`. **Lead non filé (pas de preuve live, data null)** : `GET /v1/cabinets/:id/info` (route PUBLIQUE, `cabinet_info.rs:150`) renvoie `access.door_code` **sans** gater par `is_current_patient` (calculé mais jamais utilisé) — tous les door_code seed sont NULL donc fuite non démontrable ; à rejouer quand un cabinet aura un door_code (compte admin requis). Note : `list_consultations` est cabinet-wide (non practitioner-scopé) mais consultations/acts = billing-adjacent, non filé (jugement de design). |
| couverture NOUVELLE sans écart : pharmacies list, create_cabinet_patient, notes CRUD, doc cabinet→patient | 2026-07-10T13:40Z | OK | **Tout correct.** `GET /pharmacies` liste OK. `POST /cabinet/patients` idempotent (compte Marc→même patient_id d1, 201 ; compte random→404 anti-enum). `POST /cabinet/patients/:id/notes` (praticien) : add observation→201 + round-trip GET DESC ; note_kind hors {observation,act}→422 ; texte vide→422. `POST /cabinet/patients/:id/documents` (secrétariat, multipart) upload `cr` PDF→201 → **le patient le VOIT** (`GET /documents?category=cr` présent) + **télécharge** (`/documents/:id/download`→200 signed URL) + détail 200 ; scan_status='pending' ne bloque PAS la visibilité patient (pas de cul-de-sac). `GET /account/{orders,prescriptions,pharmacy-quotes}` + `/notifications` tous triés created_at DESC, DTO complets (pharmacy_name résolu). `POST /waiting-list` (revue code) : 404 si patient hors cabinet, 409 doublon actif. Dashboard counts (revue code) : RLS-scopés, OK. |
| couverture NOUVELLE exhaustive cross-rôle 17:00Z — 7 flux, **tous CLEAN** (pickup-scan, acts→devis, stock-requests, pharmacy-quotes, reviews, waiting-list, notifications) | 2026-07-10T17:00Z | OK | **Aucun écart vs le design.** (1) **pickup-scan** (patient→pharma) : `GET /account/orders/:id/pickup-token` ready→200 / preparing→409 / pharma-token→403 ; `POST /pharmacy/orders/pickup-scan` token inconnu→404, vide→422, patient-token→403, bon token→200 picked_up (PII `Marc D.`), re-scan→409, re-token sur picked_up→409. (2) **consultation-acts→complete→devis** (praticien) : `POST /cabinet/consultations/:id/acts` secrétaire→403, montant<0→422, 2 actes 2800+5000 persistés ; `complete`→200 `next_step:sign_quote` + RDV→done + re-complete→409 ; devis généré `draft` total 7800 = somme des lignes (2800+5000), **invisible patient** (`GET /quotes/:id`→404, correct #3487) jusqu'au send. (3) **stock-requests** (cabinet↔pharma) : qty≤0/items vide→422 ; create→sent, pharma f1 le voit ; fulfill-sur-sent→409, accept→accepted, cancel-cabinet-sur-accepted→409, double-accept→409, fulfill→fulfilled ; **cross-tenant** f1 accept/reject demande f2→404 ; pharma-token sur endpoint cabinet→403, patient sur endpoint pharma→403. (4) **pharmacy-quotes** (officine→patient) : prix<0→422 ; create draft→**invisible patient** (accept draft→404) ; send→sent visible patient ; accept→accepted, re-accept→409, refuse-accepted→409. (5) **reviews** (patient→cabinet) : rating 6→422, sans Idempotency-Key→400, doublon RDV→409 ; pending **absent** de `GET /providers/:id/reviews` public ; modération bad-status→422, **secrétaire publish→200 (par design, doc reviews.rs:298)**, praticien re-modère→404, avis apparaît public après publish. (6) **waiting-list** : provider inconnu→404, join→201 active, doublon→409, cabinet voit l'entrée, offer bad-date→422, offer→200 `notified:true` + notif patient `waiting_list_slot_offered` livrée (#3480 fix live), re-offer fulfilled→409. (7) **notifications** : `POST /notifications/:id/read`→is_read+read_at, `read-all`→`{updated:5}` unread→0. **Cross-rôle propagation confirmée** : mes actions (pharmacy_quote_sent, order picked_up, waiting_room_called) toutes remontées en notifs patient. Validation batch OK : referring-doctor (both/neither→422, provider inconnu→404), account/pharmacy PUT inconnu→404, search/suggest préfixe (ortho→Orthodontie, implant→Implantologie), pro/verification→verified. |
| PRIORITÉ 1 file-attente — **#3477 fix CONFIRMÉ LIVE** 17:00Z (start accepte in_progress) | 2026-07-10T17:00Z | OK | Waiting-room avait 3 RDV coincés `in_consultation` (wait 439-499min). `POST /cabinet/appointments/:id/start` sur RDV `in_progress` (d1550525, 51abc235) → **200** `{consultation_id,status:in_progress}` (avant : 409 invalid_status). Complétés → `complete`→200, **sortis de la waiting-room** (compte 0 après). Le RDV avec actes (2c2725a7) sorti aussi après complete. **Flux queue entièrement SORTABLE, pas de cul-de-sac** sur le deploy courant (dernier succès 2026-07-09T21:30Z). |
| reconfirmations live 17:00Z (issues ouvertes, non re-filées) | 2026-07-10T17:00Z | bug | **#3542** re-confirmé : `GET /cabinet/agenda` liste **16 praticiens** (Amélie Dubois, Antoine Mercier… `90c70000-*` marketplace) alors que `GET /cabinet/members`=1 → fuite cross-cabinet toujours live. **#3543** re-confirmé : `GET /reminders` sert des rappels **mockés en dur** (id `a1b2c3d4-…`, "Dr. Dubois", due_at passés) ; dashboard `reminders:0` ≠ endpoint → incohérence symptôme du mock. **#3546** re-confirmé : `to_pay` du dashboard patient contient des paiements `amount_cents` **-5000 / 0 / 999999** (artefacts create sans validation `billing.rs` `create_payment_intent`, aucune borne signe/plafond). Tous déjà ouverts, aucune re-file. |
| couverture NOUVELLE 18:00Z — endpoints jamais exercés + PRIORITÉ 2 pharmacie E2E, **tous CLEAN** | 2026-07-10T18:00Z | OK | **Aucun écart vs le design, 0 finding NOUVEAU.** (1) **notification-preferences** (patient) : GET défauts tous-true, PATCH partiel `email_rdv:false` persiste (round-trip GET), type invalide `"yes"`→422, restore→200. (2) **consents RGPD** : GET liste, PUT toggle marketing persiste ; purpose arbitraire accepté (déjà noté P3 non-filé, `consent_record.purpose text` sans CHECK `0008:41`). (3) **account/pharmacy** : GET déclarée f1, PUT pharmacie inconnue→404, uuid malformé→422. (4) **treatment-plans** : list+detail 200, id random→404 ; total_cost 268000=Σ unit (qty=1) — **bug qty latent `treatment_plans.rs:289` non prouvable live** (aucun chemin API ne pose qty>1, plan pré-seedé) → non filé (règle preuve). (5) **implant-passport/export** + **documents/:id/download** : 302/200 vers `storage.example.com?token=stub` = StorageSigner mock env-wide, by-design MVP (déjà #3497), non filé. (6) **RBAC secrétaire** : `POST/DELETE /cabinet/secretariats`→403, `POST /cabinet/members`→403, `GET /cabinet/secretariats`→200 (member). (7) **cloisonnement clinique** : secrétaire `medical-record`/`notes`/`dental-chart`→403, `documents`→200 ; praticien `medical-record`→200. (8) **consultation-acts** : acte sur consultation `completed`→409, montant<0→422, ccam vide→422, secrétaire→403. (9) **reviews** : rating 9→422, RDV `requested`→422 `appointment_not_honored`, sans Idempotency-Key→400. (10) **PRIORITÉ 2 pharmacie E2E (token pharma RÉEL f1, seed jean)** : kind-sep patient/pharma/pro→403 croisés, jean→f2 `no_membership` 403 ; **cloisonnement dur : f1 GET/accept commande d'une autre pharmacie→404 anti-énum** (3 ids testés) ; machine à états `received→accept→preparing→ready`, transitions invalides (accept preparing / ready picked_up / reject cancelled)→409 ; **pickup-scan** token→picked_up 200, re-scan→409 (single-use) ; PII minimisée `Marc D.`. (11) **misc** : referring-doctor/avatar GET, notifications read-all `{updated:6}`→unread 0, provider/listing PUT praticien→403 (admin-only design), search/providers+availability+cabinet_info 200. **DEPLOY RÉTABLI** : succès `19446`@17:09Z + `19460`@17:29Z (fin outage #3554, dernier échec avant = 07-10 10:37Z) → **live == main confirmé** via #3520 : `POST /account/prescriptions/:id/order` dupliqué renvoie désormais `{"code":"already_ordered"}` 409 (comportement FIXÉ PR #3534/commit `4865c984`, avant l'outage = `invalid_status`). Donc résultats CLEAN de ce run à jour vs main. |
| couverture NOUVELLE 18:50Z — validation dépendants + booking cross-cabinet vérifié + E2E pharmacie frais | 2026-07-10T18:50Z | bug | **1 finding NOUVEAU : #3573 (P3)** — `POST`/`PATCH /account/dependents` accepte une `birth_date` dans le **futur** (`2099-01-01`→201, persistée+relue via GET) ; root cause `auth/mod.rs:3414-3417` + `patch_account_dependent` = `parse`-only sans check `<= now()` (symptôme distinct de #3526 nom vide). Le reste **CLEAN vs design** : (1) **consents** revoke round-trip (`marketing granted:false`→`revoked_at` posé, re-grant OK), body sans `granted`→422. (2) **notification-preferences** PATCH partiel `sms_rdv/push_messagerie:false` persiste (GET reflète). (3) **pharmacie click-and-collect E2E FRAIS** (ordonnance neuve `db93c1a2` : praticien create→sign→send f1 ; order `7944d9aa` received, PII `Marc D.`) : accept→preparing, reject-sur-preparing→409, ready, patient cancel-sur-ready→409, pickup-token→pickup-scan→picked_up, double-scan→409 ; GET order/document 200. (4) **treatment-plan detail** totaux exacts (268000=Σ, remaining 141250, amo 13750, amc 113000 — qty=1 seed, bug qty toujours latent/non-prouvable). (5) **devices** register 201 / platform invalide→422 / unregister 204 / idempotent 204. (6) **reviews** rating bornée 1-5 (`reviews.rs:58`). (7) **clinique cloisonnement** secrétaire `medical-record`/`dental-chart`/`notes`→403, `documents`→200 ; praticien→200. (8) **agenda cabinet — RLS cross-cabinet VÉRIFIÉE (#3542 fix tient)** : liste praticiens = c1/c2 seulement (plus les 14 Paris) ; **booking FRAIS d'un provider Paris (Girard `90de…06`) → cabinet `Cabinet Girard`, N'APPARAÎT PAS dans l'agenda Lyon** (praticien+secrétaire, date 07-13) → isolation OK, stamping cabinet correct (`bookings.rs`+`resolve_slot_for_booking` cohérents cabinet/practitioner). Note : 1 appointment orphelin héréditaire subsiste dans l'agenda Lyon (`142536a7`, cabinet=Lyon mais practitioner=Girard-Paris) = **donnée corrompue legacy** (ère outage deploy 07:02-10:37Z, code depuis corrigé) — NON reproductible sur main→live actuels, non filé (règle root-cause actuel). (9) **waiting-room privacy-by-role** : secrétaire→initiales `MD`, praticien→nom complet (`scheduling.rs:600-608`, by-design). (10) **put_account_pharmacy** valide pharmacie `is_listed`→404 sinon ; **patch_appointment** cross-patient→404 (RLS). (11) **/dashboard** = patient-only (praticien→403). Validation notée non-filée : dépendant birth_date futur = #3573 ; purpose consent libre (P3 connu). **DEPLOY** dernier succès 17:58Z (live≈main). |
| couverture NOUVELLE 20:15Z — §14 consultations (by-design) + note de séance absente + agrégat note annuaire figé + #3477 reconfirmé | 2026-07-10T20:15Z | bug | **2 findings NOUVEAUX**. **#3575 (P3)** la note de séance clinique chiffrée est inenregistrable/introuvable : `PUT /cabinet/consultations/:id/note` (doc 12:398) **absent de `lib.rs:365-383`**, aucun handler n'écrit `note_ciphertext`, `get_consultation_context` (`consultations.rs:113`) renvoie `note:null` en permanence, front `consultation_clinique` sans éditeur (aucun `SaveNote`). **#3576 (P2)** un avis publié ne met jamais à jour `provider.rating_avg`/`review_count` (colonnes seed-only `0009_marketplace.sql:70-71`, **0 écriture** dans `api/src/` ; ni `create_review` ni `moderate_review`) — prouvé live E2E : avis 1★ publié (RDV frais `9ee0ff95`→confirm→checkin→review→PATCH published) → `/reviews` 4→5 (moy réelle 4.5→3.8) mais carte `providers/:id` reste **4.8/124** inchangée ; le tri marketplace `search_providers` (`marketplace.rs:523`) s'appuie sur cette note figée. **CLEAN vs design** : (1) **§14 consultations** — `list_consultations`/`get_consultation_context`/`list_consultation_acts` cabinet-scoped sans garde §14 (Hugo lit la consultation de Camille, patiente de Claire sans RDV avec lui : notes→403 mais consultation detail+acts→200) MAIS `docs/12-api-reference.md:394` documente « Historique des séances **du cabinet** » → **by-design, non filé** ; l'écriture est gardée (`add_consultation_act:470-483` « seul le praticien qui a démarré »→403). (2) **#3477 reconfirmé fixé** : RDV `in_progress` avec session existante → `start`→409 (protection double-séance légitime `start_consultation:1412-1422`), sortie via `complete` sur la session→200 done → sort de la waiting-room (nettoyé le fantôme `6650f362`/session `766fb60a`). (3) **FIFO** `get_waiting_room` (`scheduling.rs:531`) et `call_next_patient` (`:343`) trient identiquement `checkin_at ASC NULLS LAST, starts_at ASC` — cohérent. (4) **cabinet/patients** liste sans doublon. (5) **treatment-plans** GET-only (write API absente → bug qty toujours latent) ; **waiting-list** patient POST-only (pas de GET/DELETE patient, latent cf #3500). **Lead non filé** : `patch_cabinet_appointment` (`scheduling.rs`) ne notifie pas le patient d'une reprogrammation, mais aucune exigence doc explicite (spéculatif). **DEPLOY** dernier succès 19764 @19:11Z (live≈main frais). |

Last run cross-rôle: 2026-07-11T06:35Z. **3 findings NOUVEAUX (backend, scope:rust), 3 surfaces jamais auditées.** **#3612 (P1, cloisonnement clinique WS)** — `GET /v1/ws` jamais testé : `authorize_conversation` branche pro (`realtime/mod.rs:438`) n'a ni filtre de scope ni rôle (`WsSession` sans `role`) → un secrétaire s'abonne à `conversation:<fil clinique c1000000-…-04>` et reçoit en temps réel `message_created`/`read` (message_id+sender_kind+timing), alors que le REST lui renvoie 404 (§07 §4.1, fix #3596). Prouvé E2E : subscribe secr=subscribed ; praticien POST msg=201 ; WS secr reçoit `{event:message_created, sender_kind:practitioner}`. Jumeau WS du fix #3596 (métadonnées, pas le corps → P1). **Corrigé+mergé pendant le run (PR #3613).** Contrôles sains : cross-patient/pharma/kind tous `forbidden`, no/bad token rejeté. **#3614 (P2, machine à états)** — `create_cabinet_appointment` (`scheduling.rs:888`) résout le créneau (`:918`) sans check `status`, force `SET status='booked'` (`:968`) → secrétaire pose un walk-in sur créneau `blocked` (indispo détruite) OU `held` (hold patient volé, patient book ensuite=409). Prouvé : blocked→hold patient 404 mais create=201 ; held→create 201 puis patient book 409. Chemin patient protégé par le hold (`claim_and_hold_slot` status='open'), walk-in sans hold court-circuite. **#3616 (P2, donnée fausse publique)** — `search_providers` `next_slot_at` (`marketplace.rs:563`) + `?available` (`:535/:542`) filtrent `status='open'` sans `online_booking=true`/`deleted_at IS NULL` → résidu non corrigé de #3608/#3609. Prouvé public : slot non-online→`next_slot_at`=07-12T08:00 (hold=404, absent search_slots) ; DELETE→`next_slot_at` toujours 07-12T08:00. Carte praticien annonce une dispo non réservable/inexistante. **RE-VÉRIF FIXES LIVE (deploy `19990`@05:43Z)** : #3604 FIXÉ (reschedule libère l'ancien slot : re-hold→200), #3606 FIXÉ (hold expiré→re-hold 200 après TTL), #3608 FIXÉ (non-online absent search_slots+hold 404), #3609 FIXÉ (deleted disparaît search_slots), get_provider_availability porte les 3 filtres+LIMIT 50. Prochain run : cross-cabinet clinique §14 (2e cabinet) ; door_code public quand clé réelle ; WS `patient_queue` cross-praticien (payload sans PII, à confirmer) ; provision_staff kind-filter (admin requis) ; re-vérifier #3612/#3614/#3616 corrigés + #3546/#3553/#3555/#3556/#3558.

Last run cross-rôle: 2026-07-11T03:25Z. **2 findings NOUVEAUX, famille 'slot lifecycle desync' (backend, scope:rust).** **#3604 (P2)** reschedule (`PATCH /v1/appointments/:id`, `appointments.rs:32`) ne libère jamais l'ancien créneau : le SELECT (l.55) ne lit pas `slot_id`, l'UPDATE ne touche que starts_at → créneau orphelin `booked` (indispo fantôme) + `slot_id` divergent de `starts_at` ; même gap `patch_cabinet_appointment` (`scheduling.rs:1498`) ; contraste cancel (`:261`)/booking (`bookings.rs:176`) gèrent le créneau ; distinct de #3558. Prouvé live (slot 510ad11c : reschedule→re-hold 409 slot_taken, cancel libère→200). **#3606 (P1)** hold expiré jamais libéré → créneau `held` à vie : `claim_and_hold_slot` (mig 0120) ignore `slot_holds.expires_at`, hold DELETE seulement à la réservation (`bookings.rs:186`), aucun reaper (`StubJobDispatcher` no-op `lib.rs:88-93`), « expiration automatique » de mig 0095 non implémentée ; chaque hold abandonné stérilise un créneau (invisible, toutes dispos filtrent status='open'). Prouvé live (slot d6fc88ec : hold TTL 5min → post-expiry book=409 hold_invalid, re-hold=409 slot_taken). **RE-VÉRIF FIXES LIVE (deploy 19948@03:22Z)** : #3602 FIXÉ (secrétaire→carte_mutuelle only, catégorie clinique explicite→vide ; praticien non-suivant→403 sur 4 familles) ; #3596 FIXÉ (secrétaire fil clinical→404) ; #3559 FIXÉ (§14 4 endpoints) ; #3550 FIXÉ (deleted_at slots) ; #3548 FIXÉ (amount<=0/deposit_pct). **CLEAN** : idempotency booking cross-compte étanche (lookup RLS-scopé compte — pas de fuite type #3547), `patch_cabinet_slot` refuse booked, act patch/delete gardés in_progress, sign_quote exige sent, offer_waiting_list proposed_at>now+5min. **Ouverts re-confirmés** : #3543 (reminders mockés), #3592 (cabinet info null). Prochain run : cross-cabinet clinique §14 (2e cabinet) ; door_code public quand clé réelle ; RDV placé sur période `blocked` via cabinet-reschedule (non validé) ; re-vérifier #3604 corrigé (reschedule libère l'ancien slot) & #3606 corrigé (hold expiré → re-hold 200) + #3546/#3553/#3555/#3556/#3558.

Last run cross-rôle: 2026-07-11T02:07Z. **1 finding NOUVEAU : #3602 (P1 cloisonnement clinique §14 documents).** `GET /v1/cabinet/patients/:id/documents` (`list_patient_documents`, `clinical.rs:1030`, `ProSecretaryPlusClaims`) est le **4e endpoint clinique** cabinet→patient et n'a **ni garde §14** (relation de soin) **ni filtrage catégorie** — root cause explicitement signalé dans le commentaire de #3559 (2026-07-10T14:43Z) mais laissé hors-scope du fix PR #3582 (medical_record+dental_chart seulement). Prouvé live : (A) praticien SANS RDV avec d4 → notes/medical-record/dental-chart=403 mais documents=**200** ; (B) secrétaire (403 partout sur le clinique) → documents de d1=**200** avec catégories `cr`/`radio`/`ordonnance` (fuite métadonnées). Sibling correct = `list_patient_notes` `clinical.rs:470-487` (`has_appointment`+`ProPractitionerClaims`). `upload_patient_document` (POST `clinical.rs:1200`) = même gap write, même root cause. Filé `scope:rust`/`rust-agent` (comme #3596, backend pur — évite la churn mislabel scope:flutter-front). **Reste CLEAN** : **messagerie pharmacie** — `get_pharmacy_conversation_messages`/`mark_pharmacy_conversation_read` (`pharmacy/messaging.rs:132/272`) omettent le filtre `AND pharmacy_id=$2` MAIS RLS 0126 (`conversation_pharmacy_all`/`message_pharmacy_all`) fail-closed couvre (pharma f1 → fil cabinet-clinical/patient-cabinet/inexistant = 404) ; mark-read sans bug UUID #3555. Deploy sain (dernier succès 01:13Z, live≈main). Prochain run : (1) re-vérifier que #3602 est corrigé (documents=403 pour non-suivant/secrétaire) ; (2) cross-cabinet clinique §14 (2e cabinet) ; (3) R10 secrétariat-scope détail conversations (2e secrétariat/admin) ; (4) door_code public quand clé `door_code` réelle ; (5) re-vérifier #3546/#3553/#3555/#3556/#3558/#3586/#3587. **[ARCHIVE run 00:14Z ci-dessous]** Vein très productif = **audit de contrat front↔backend (query-params/méthode)** + **cloisonnement clinique messagerie**, 6 findings NOUVEAUX. **#3596 (P0)** cloisonnement clinique cassé : un secrétaire lit ET écrit les fils `scope='clinical'` par ID (`cabinet_messaging.rs:379/446/532` gardent seulement `id+cabinet_id`, sans filtre scope, alors que la liste bloque à 403 ; aucune RLS DB sur scope) — prouvé live sur `c1000000-…-04` (Marc Dubois) : GET messages→200, POST→201, /read→204. **#3594 (P1)** liste devis cabinet morte : front `CabinetQuotesApi.list` envoie `?page=1` que `list_cabinet_quotes` (`billing.rs:1042`) rejette en 400 → écran Devis en erreur permanente (praticien+secrétariat). **#3593 (P2)** historique RDV patient tronqué à 20 (front `getHistory` envoie `page` ignoré, backend pagine par `cursor` ; 110 RDV total, page1==page2==page3). **#3598 (P2)** coffre-fort documents tronqué à 20 (29 docs, front `getAll` ignore `next_cursor`) — même famille pagination. **#3592 (P3)** `/cabinets/:id/info` public renvoie horaires/parking/pmr/door_code tous null (mauvaises clés JSON `cabinet_info.rs:112-121`) + door_code non gaté par is_current_patient (fuite latente masquée par le bug de clé). **#3597 (P3)** message patient vide accepté (201) vs cabinet 422. Latents non filés (code mort front) : create/update devis (toJson sans items), book (sans provider_id), cabinet getById (route PATCH-only 405). Notifications = même plafond 20 (non filé, mitigé read-all). Re-confirmés FIXÉS live : **#3545** (patient_name 149/150), **#3557** (deeplink directions peuplé), **#3575** (set_consultation_note round-trip + RBAC secr/patient 403 + 404 inconnu). **#3558 re-vérif inconclusive** (garde too_late masque). Deploy sain (`19820`@23:06Z, live≈main). Prochain run : R10 secrétariat-scope sur détail conversations (2e secrétariat/admin requis) ; cross-cabinet clinique §14 (2e cabinet) ; door_code public quand une clé `door_code` réelle existera ; brancher la pagination cursor côté front (RDV/docs/notifs) une fois #3593/#3598 corrigés ; re-vérifier #3546/#3553/#3555/#3556/#3558/#3586/#3587.

Last run cross-rôle: 2026-07-10T20:15Z. Priorité au NOUVEAU non couvert : **surfaces cliniques consultations + agrégats annuaire**. 2 findings NOUVEAUX, root cause dans du code jamais corrigé sur main (live-testables) : **#3575 (P3)** note de séance clinique chiffrée inenregistrable/introuvable — route `PUT /cabinet/consultations/:id/note` (doc 12:398) absente de `lib.rs:365-383`, aucune écriture de `note_ciphertext`, `get_consultation_context` (`consultations.rs:113`) renvoie toujours `note:null`, front `consultation_clinique` sans éditeur (aucun `SaveNote`/`TextField`). **#3576 (P2)** avis publié sans effet sur la note affichée — `provider.rating_avg`/`review_count` seed-only (`0009:70-71`), **0 écriture** dans `api/src/`, ni `create_review` ni `moderate_review` ne recalculent ; prouvé live E2E (avis 1★ publié → `/reviews` 4→5 moy 3.8 mais carte `providers/:id` **4.8/124 inchangée** ; tri marketplace `marketplace.rs:523` sur note figée). CLEAN vs design : **§14 consultations cabinet-wide = by-design** (`docs/12-api-reference.md:394` « historique des séances du cabinet » ; écriture gardée `add_consultation_act:470`), **#3477 reconfirmé fixé** (start sur in_progress+session→409 = protection double-séance correcte, complete sort de la file ; fantôme `6650f362` nettoyé), FIFO ordering identique `get_waiting_room`/`call_next_patient`, `cabinet/patients` sans doublon, treatment-plans GET-only (bug qty latent), waiting-list patient POST-only (latent cf #3500). Lead non filé : `patch_cabinet_appointment` ne notifie pas d'une reprogrammation (pas d'exigence doc explicite → spéculatif). **DEPLOY** dernier succès 19764 @19:11Z (live≈main frais). Prochain run : cross-cabinet clinique §14 (2e cabinet) ; door_code public `cabinets/:id/info` (admin) ; consents/treatment-plans cross-patient (2e compte patient) ; re-vérifier #3547/#3553/#3555/#3556/#3558/#3559/#3575/#3576.

Last run cross-rôle: 2026-07-10T18:50Z. **1 finding NOUVEAU : #3573 (P3, `POST`/`PATCH /account/dependents` accepte une birth_date future — root cause `auth/mod.rs:3414-3417` parse-only sans check `<=now()`).** Le reste **CLEAN vs design** : consents (revoke round-trip + `granted` requis 422), notification-preferences (PATCH partiel persiste), **pharmacie click-and-collect E2E FRAIS** (ordonnance neuve create→sign→send→received→accept→ready→pickup-scan→picked_up + edges reject-preparing/cancel-ready/double-scan tous 409, PII `Marc D.`), treatment-plan totaux exacts (qty=1, bug qty toujours latent/non-prouvable), devices CRUD (register/422/unregister idempotent), reviews rating 1-5, clinique cloisonnement (secrétaire 403 medical-record/dental-chart/notes, 200 documents), **agenda cross-cabinet RLS VÉRIFIÉE (#3542 fix tient)** : booking frais d'un provider Paris (Girard `90de…06`)→cabinet Girard, N'APPARAÎT PAS dans l'agenda Lyon (isolation OK, stamping cabinet cohérent `bookings.rs`+`resolve_slot_for_booking`), waiting-room privacy-by-role (secrétaire initiales/praticien nom complet), put_account_pharmacy 404, patch_appointment cross-patient 404 RLS, /dashboard patient-only. **Legacy non filé** : 1 appointment orphelin `142536a7` (cabinet=Lyon, practitioner=Girard-Paris) subsiste dans l'agenda Lyon = donnée corrompue de l'ère outage 07:02-10:37Z, NON reproductible sur code actuel (règle root-cause actuel). Non filés connus : treatment-plan qty (masqué qty=1), purpose consent libre (P3). **DEPLOY** dernier succès 17:58Z (live≈main). Prochain run : cross-cabinet clinique §14 (#3542, 2e cabinet requis) ; door_code public `cabinets/:id/info` (admin) ; treatment-plans/consents cross-patient (2e compte patient) ; nettoyage des RDV fantômes checked_in/in_progress (588min) + orphelin `142536a7` ; re-vérifier #3547/#3553/#3555/#3556/#3557/#3558/#3559.
<!-- ARCHIVE run précédent 17:00Z : 7 flux CLEAN — **pickup-scan** (QR retrait patient→pharmacie, cross-tenant+kind-sep étanches), **consultation-acts→complete→devis** (praticien : RBAC/validation/génération devis draft cohérent 2800+5000=7800, invisible patient jusqu'au send), **stock-requests** (cabinet↔pharmacie : machine à états complète sent→accepted→fulfilled + cancel + cross-tenant 404 + RBAC 403), **pharmacy-quotes** (officine→patient : draft invisible→send→accept/refuse, gardes 409), **reviews** (patient→cabinet : create+modération+publish, secrétaire publish par design), **waiting-list** (join→cabinet→offer→notif patient livrée #3480), **notifications** (read/read-all cohérents). **PRIORITÉ 1 file-attente : #3477 CONFIRMÉ FIXÉ LIVE** — `POST /cabinet/appointments/:id/start` accepte `in_progress`→200 (crée session), RDV complétés sortent de la waiting-room (compte 0), flux entièrement sortable. Cross-rôle propagation vérifiée (mes actions → notifs patient correctes). **Reconfirmations live (ouvertes, non re-filées)** : #3542 (agenda 16 praticiens vs 1 membre), #3543 (reminders mockés en dur), #3546 (paiements -5000/0/999999 dans to_pay). Consent accepte purposes arbitraires (déjà pollué par runs antérieurs → P3 mineur, non filé, endpoint interne cohérent). **Deploy toujours en panne (#3554, dernier succès 19361/19573 07-09T21:30Z)** → live = main d'hier soir ; les flux testés ce run sont donc à jour. Prochain run : cross-cabinet clinique §14 via praticiens Paris fuités (#3542, 2e cabinet requis) ; door_code public `cabinets/:id/info` (compte admin) ; `list_consultations` cabinet-wide ; treatment-plans/implant-passport cross-patient (2e compte patient requis) ; re-vérifier une fois #3554 résolu (#3547/#3520/#3546/#3553/#3555/#3556/#3557/#3558/#3559). -->


Last run cross-rôle: 2026-07-10T14:45Z. Priorité au NOUVEAU non couvert : **cloisonnement clinique §14 (relation de soin praticien↔patient)**, **FIFO waiting-room date-scope**, **dependents CRUD**. 1 finding NOUVEAU, root cause dans du code jamais corrigé → bug live authentique indépendant du deploy #3554 : **#3559 (P1)** — le garde §14 (au moins un RDV entre CE praticien et le patient, appliqué par `list_patient_notes` `clinical.rs:471-486` → 403) est **absent** de `get_medical_record`/`patch_medical_record` (`medical_record.rs`), `get_dental_chart`/`put_dental_chart` (`dental_chart.rs`) et `list_patient_documents` (`clinical.rs`) : prouvé live sur 2 patients sans RDV avec Hugo (`notes`=403 vs `medical-record`/`dental-chart`/`documents`=200, lecture ET écriture). Un praticien accède/modifie le dossier médical + schéma dentaire + documents de patients qu'il n'a jamais soignés (intra-cabinet, §14). Investigations sans finding : **FIFO waiting-room** — écart de code réel (`get_waiting_room` filtre `checkin_at IS NOT NULL` + `starts_at` today, mais `call_next_patient` `scheduling.rs:342/354` ne filtre NI l'un NI l'autre → un `checked_in` invisible dans la file est appelable) mais **non reproductible live** (checkin exige d'être in-window le jour même ; post-checkin le PATCH date→409 ; les 3 lignes seed `checked_in`+`checkin_at NULL` sont côté praticien c2 sans token dispo) → non filé faute de preuve ; cycle call-next→start→complete re-confirmé OK (#3477 toujours fixé). **Dependents** cloisonnement OK en lecture code (`get/patch/delete_account_dependent` scopés `guardian_account_id`, cross-compte→404). **Faux-positif écarté** : `list_appointments` patient renvoie bien le nom praticien (`provider.display_name` imbriqué « Dr Hugo Marin », vérifié raw JSON — erreur de parse initiale). Marketplace/professions/specialties/ccam-acts/me/today-notes OK. **Lead** : `GET /cabinets/:id/info` public expose `access.door_code` non gaté (data seed null → non prouvable). **Deploy toujours en panne (#3554 open, dernier deploy réel 19629 échec 10:38Z, dernier succès 19573 07-09T21:35Z)** : fixes 07-10 non live ; #3559 est en code jamais corrigé donc live-testable maintenant. Prochain run : cross-cabinet clinique via praticiens Paris fuités (#3542, 2e cabinet) ; door_code public (compte admin) ; `list_consultations` cabinet-wide §14 ; re-vérifier une fois #3554 résolu (#3547/#3520/#3546/#3553/#3555/#3556/#3558/#3557).

Last run cross-rôle: 2026-07-10T13:40Z. Priorité au NOUVEAU non couvert : **reprogrammation RDV patient (validation starts_at)**, **itinéraire/préparation (résolution adresse)**, **surfaces jamais testées** (pharmacies, create_cabinet_patient, notes CRUD, doc cabinet→patient). 2 findings NOUVEAUX, root cause non corrigée sur main, live-testables indépendamment du deploy #3554 : **#3558 (P2→P1)** `PATCH /appointments/:id` accepte n'importe quel `starts_at` (passé/hors créneau) sans validation (`appointments.rs:88-104`) → RDV fantôme définitivement bloqué visible côté cabinet ; même défaut sur `patch_cabinet_appointment` (`scheduling.rs:1520`). **#3557 (P3)** `directions`/`preparation` lisent `cabinet.settings.address` (vide pour le cabinet seed) au lieu de `establishment.address`/provider (peuplés, exposés par `providers/:id`) → deeplink itinéraire vide + préparation sans adresse. Large couverture NOUVELLE **sans écart** : `GET /pharmacies`, `POST /cabinet/patients` (idempotence+anti-enum), notes CRUD (validation+DESC), **document cabinet→patient** (upload secrétariat→visible+téléchargeable côté patient, pas de cul-de-sac scan_status), listes patient (orders/prescriptions/pharmacy-quotes/notifications toutes DESC + DTO complets), waiting-list join (revue), dashboard counts (revue). **Deploy toujours en panne (#3554, dernier succès 19573 07-09T21:35Z)** : fixes 07-10 non live. Prochain run : cross-cabinet clinique via praticiens Paris fuités (#3542, 2e cabinet requis) ; FIFO waiting-room multi-patient (2 patients checked_in F1 in-window) ; dependents CRUD cloisonnement ; re-vérifier une fois #3554 résolu (#3547/#3520/#3546/#3553/#3555/#3556).

Last run cross-rôle: 2026-07-10T12:35Z. Priorité au NOUVEAU non couvert : **surfaces d'ÉCRITURE jamais testées** (avatar, dental-chart PUT, medical-record PATCH, coverage-card multipart) + **messagerie cabinet triage / accusé de lecture**. 2 findings NOUVEAUX (P2, root cause non corrigée sur main, live-testables indépendamment du deploy #3554) : **#3555** `mark_conversation_read {last_read_message_id}` compare des UUID `gen_random_uuid()` avec `id <= $2` (`messaging.rs:470`) → marque les MAUVAIS messages lus (prouvé : unread 6 au lieu de 0 ; les 6 UUID lexicographiquement > la borne restent non lus) ; latent car le front envoie un corps vide (branche None correcte). **#3556** `list_cabinet_conversations` dérive le triage du fil du SEUL dernier message (`cabinet_messaging.rs:181-192`) → un message patient urgent non lu perd son badge « Urgent » dès qu'un message normal suit (défait le tri `urgency_int ASC` et la fonction de triage). Large couverture NOUVELLE **sans écart** : avatar PUT/GET + validation, dental-chart PUT (atomique, RBAC, cross-404), medical-record PATCH merge partiel + cloisonnement, coverage-card (EICAR/side/mime→422), triage propagation urgent→cabinet, audit deleted_at (prescription/quote/appointment/treatment_plan tous filtrés). Écart dashboard/conversations unread pharmacie = by-design (non filé). **Deploy toujours en panne (#3554)** : les fixes 07-10 (#3552/#3549/#3534/#3533/#3530) restent non live. Prochain run : `create_cabinet_patient` idempotence/validation ; `upload_patient_document` (cabinet→patient) cloisonnement ; `waiting_list` validation ; cross-cabinet clinique via praticiens Paris fuités (#3542) ; re-vérifier une fois #3554 résolu.

Last run cross-rôle: 2026-07-10T11:30Z. Priorité au NOUVEAU non couvert : **surface auth complète** (MFA enroll/verify, password reset, refresh, select-context/pharmacy-context), **webhooks HMAC**, **pharmacie E2E token pharma réel**, **verification/listing annuaire**. 2 findings NOUVEAUX : **#3553 (P1)** MFA totalement inopérante — `mfa_verify` écrit `mfa_enabled/mfa_secret` (mig 0002) mais `login` lit `totp_enabled/totp_secret` (mig 0014), 2 paires de colonnes disjointes jamais synchronisées → login n'exige JAMAIS le code même après « MFA activée. » ; prouvé live E2E sur pro jetable ; root cause code **non corrigé sur main** (bug authentique, indépendant du deploy). **#3554 (P0 infra)** deploy en échec continu depuis 07:02Z (LXC `DEPLOY_HOST:22` injoignable, preflight KO ~84s, dernier succès 19573 07-09T21:35Z) → aucun fix mergé depuis n'atteint le live ; assigné rust-agent. Large couverture NOUVELLE **sans écart** : webhooks Stripe/Yousign/GoCardless (no-sig/bad-sig→401), cloisonnement select-context/pharmacy-context (foreign→403 no_membership, inexistant→404, patient/MFA-enroll patient→403), password-reset/refresh (revue code : rotation + replay→revoke-chaîne + expiry + single-use corrects), notif-prefs partial upsert (round-trip live, préserve absents), pharmacie click-and-collect E2E complet (token seed jean.officine), verification/listing (RPPS gate 409, admin-only 403), billing cross-compte étanche (patient B sur devis A→404). **Deploy-lag re-confirmé** : #3547 (fingerprint, PR #3549) et #3520 (already_ordered) encore pré-fix en live → commenté #3547, tracké globalement #3554 ; #3546 (amount validation) toujours non corrigé sur main (open). `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** (13e run) → contourné via seed `jean.officine@pharmacie-lyon.test` (pharma token f1 OK). Prochain run : re-vérifier une fois #3554 résolu (#3547/#3520/#3546 + MFA #3553 est déjà live-testable) ; cross-cabinet clinique via praticiens Paris fuités (#3542) ; cabinet messaging triage ; avatar upload ; medical-record/dental-chart write round-trip.

Last run cross-rôle: 2026-07-10T10:40Z. Priorité au NOUVEAU non couvert : **domaine cabinet slots CRUD** (jamais testé en API) + **anti-énumération cross-patient** (2e compte patient enfin créé via `/auth/register`). 2 findings NOUVEAUX filés : **#3550 (P2)** créneau supprimé (soft-delete) reste listé côté cabinet ET holdable côté patient → cul-de-sac au booking (404) ; root cause = filtre `deleted_at IS NULL` absent de `list_cabinet_slots`/`claim_and_hold_slot` (présent ailleurs). **#3551 (P1)** créneau `status:"blocked"` → 500 (CHECK DB `0009:81` jamais élargi à 'blocked' alors que le handler l'accepte). Les DEUX ont leur root cause dans du code/migrations anciens **non corrigés sur main** → bugs live authentiques (pas des artefacts de déploiement). Large couverture NOUVELLE **sans écart** : cloisonnement patient↔patient R/W (toutes ressources → 404, aucune fuite) ; RBAC clinique ordonnance (secrétariat→403) ; cycle prescription create→sign→visibilité patient ; cycle commande complet (dup/cancel/re-order/pickup-token/pharmacie préférée). **Retard de déploiement re-détecté** (pattern #3493) : le fix `already_ordered` (#3520 CLOSED, commit `4865c984` sur main du 07-09 17:36) **n'est pas servi par le live** — dup order renvoie encore `{"error":"invalid_status"}` ; commenté #3520 avec preuve live (pas de nouvelle issue, doublon fermé <24h). `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** (12e run) → pharma-token non testable. Prochain run : MFA enroll/verify ; webhooks Stripe/Yousign HMAC ; cross-cabinet clinique via praticiens Paris fuités (#3542, 2e cabinet requis) ; re-vérifier si `4865c984` (et autres fixes 07-09) sont déployés.

Last run cross-rôle antérieur: 2026-07-10T09:24Z. Priorité au NOUVEAU non couvert : **famille validation montants sur les endpoints financiers**. 3 findings NOUVEAUX (P2, tous financiers/distinct handlers) : **#3546** paiement/acompte patient sans validation de `amount_cents` (négatif/nul/aberrant persistés → pollue dashboard.to_pay) ; **#3547** idempotency-key PaymentIntent non scopée (rejeu inter-devis renvoie le paiement d'un autre devis) ; **#3548** devis cabinet accepte lignes négatives → total/reste-à-charge négatifs (praticien+secrétariat). Pattern probant : les chemins **pharmacie** valident bien les montants (`create_pharmacy_quote` rejette `<0`), seuls les chemins **dentaire/cabinet** ne le font pas. Re-vérif : **#3545 toujours live** (cabinet/appointments sans patient_name) ; cycle file-attente terminal 100% propre (cancel/complete/double-complete/start-depuis-confirmed tous conformes). Lacunes P3 non filées (noise, déjà connues) : `add_consultation_act` n'exige pas un code CCAM existant (juste non-vide) ; `PUT /account/consents/:purpose` purpose arbitraire (persisté, re-confirmé) ; reschedule/slot dans le passé. `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** (11e run consécutif). Prochain run : anti-énumération cross-patient (2e compte patient requis) ; cross-cabinet clinique via les praticiens Paris fuités (#3542) — tester si un patient Paris est lisible par un praticien Lyon (P0 potentiel) ; webhooks Stripe/Yousign HMAC ; MFA enroll/verify.

Last run cross-rôle antérieur: 2026-07-10T08:24Z. **Priorité 2 (pharmacie) enfin exécutée** via le compte seed `jean.officine@pharmacie-lyon.test`/`Nubia2026!`/`f0…f1` (`$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** de l'env). 1 finding NOUVEAU : **#3545 (P1)** `GET /cabinet/appointments` sans `patient_name` (DTO/SQL sans `LEFT JOIN patient` alors qu'agenda + cabinet-quotes le font) → écran RDV secrétariat à noms vides. Reste (large couverture NOUVELLE) **sans écart** : messagerie patient↔pharmacie (E2E + cloisonnement P0 + token-kind), paiement devis officine (**#3505 fixé**), annuaire marketplace complet, RBAC écritures cliniques+admin, reschedule state-machine, reviews-guards, documents+cloisonnement, notifications, account↔cabinet pharmacy, implant-passport/treatment-plans. **#3539 confirmé fixé** (ordre messages ASC). Piège méthodo évité : token secrétariat vide (login rate-limité au démarrage) → 401 pris à tort pour 403, re-login + re-test propre. Prochain run : anti-énumération cross-patient documents/appointments (2e compte patient) ; billing dentaire deposit/payment-intent Stripe ; générer un devis cabinet post-fix #3545 ; origine data #3542.

Last run cross-rôle antérieur: 2026-07-10T07:20Z. Priorité au NOUVEAU non couvert : **consultation acts CRUD + génération devis** (praticien, jamais testé live avant — irréprochable : validation/RBAC/state-machine/total devis exact), **cloisonnement clinique** (secrétaire/patient→403 systématique, notes §14), **réservation en ligne patient** (hold→booking + cohérence 3 vues), **referring-doctor / implant-passport / notifications / slots-validation** (tous OK). 2 findings NOUVEAUX filés : **#3542 (P2)** agenda cabinet Lyon liste 14 praticiens d'annuaire Paris — root cause DATA (`practitioner.cabinet_id=11111111` au lieu de `90ca0000`, prouvé par le check d'appartenance de create_cabinet_slot →201), code correct ; **#3543 (P3)** `GET /reminders` mocké en dur (reminders.rs:38-73), non branché front → latent. #3535 re-confirmé live (PATCH markRead→405). `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** (priorité 2 non exécutable ce run). Lacunes P3 non filées (noise) : slot dans le passé accepté (pas de garde starts_at>now), `PUT /account/consents/:purpose` purpose arbitraire. Prochain run : documents upload/download + anti-énumération cross-patient (2e compte patient requis) ; billing deposit/payment-intent (Stripe) ; conversations urgent triage re-vérif ; investiguer l'origine data de l'anomalie #3542 (chargement seed).

Last run cross-rôle: 2026-07-09T21:22Z. Vein très productif : **audit contrat front↔backend** (143 appels `_dio` vs `lib.rs`) → 3 mismatches méthode/route câblés : #3535 (notif markRead PATCH→405, P1), #3537 (reschedule RDV secrétariat 404, P1), #3536 (unregister device 404, P2). + 2 bugs live : #3539 (fil messagerie patient à l'envers DESC/ASC, P1) et #3541 (GET /queue rapporte `waiting` pour RDV `done`, P2). Couverts OK : treatment-plans détail (totaux exacts), cabinet_secretariats/provider_secretariat RBAC (admin non testable), billing quote sign, cycle file-attente frais (3 vues cohérentes, #3477 fixé), re-vérif #3503/#3483 PASS. `$CRED_PHARMACIE_*`/`$PHARMACY_ID` **toujours absents** (priorité 2 non exécutable). NB : les lignes du run 2026-07-09T14:45Z (dependents #3525/#3529, consultation CCAM, marketplace) ne sont plus sur main — état concurrent, non restauré ici. Prochain run : happy-path CRUD cabinet_secretariats si compte admin dispo ; documents upload/download ; audit des mismatches de **champs de body** sur endpoints câblés ; statuer feature-gap vs latent sur CRUD front non câblés (agenda/consultation/waiting-room).

Last run cross-rôle: 2026-07-08T22:03:00.000Z. Ce run a exécuté les deux priorités laissées par le run précédent : (1) re-vérification live de tous les tickets rouverts pendant la panne #3493 — 5/6 confirmés PASS (#3480, #3483, #3485, #3487, #3497), 1/6 toujours FAIL avec root cause précisée et bloquant identifié (#3503, boucle rust-agent/agent-planner) ; (2) exploration + test complet du flux cabinet↔pharmacy stock-requests, jamais couvert avant — backend et front pharmacie irréprochables, nouveau finding P2 sur l'absence totale d'écran cabinet (#3507). **Non couvert ce run faute de temps** : admin-membres invitation (toujours pas de compte `admin` de cabinet parmi les creds), dossier de consultation/notes cliniques (guards lus et confirmés corrects au run 17:49Z, jamais testé live). `$CRED_PHARMACIE_*`/`$PHARMACY_ID` toujours absents de l'environnement (8e run consécutif signalé) — contournement via le compte seed documenté dans `db/seed/seed.sql:431` toujours nécessaire. Priorité du prochain run : débloquer #3503 (le patch exact est fourni en commentaire, juste besoin qu'un agent l'applique) ; couvrir dossier de consultation/notes cliniques en live ; si un compte admin cabinet devient disponible, couvrir admin-membres invitation.

---

> **Avant d'explorer / d'ouvrir une issue : lire `qa/route-manifest.md`** —
> liste des routes à tester (auth, stratégie d'URL, contenu attendu) et
> **règles anti-faux-positifs**. La plupart des issues fermées venaient de ne
> pas les respecter (route authed testée déconnectée, CSS `::placeholder`,
> mauvaise stratégie hash/path, env déployé en retard sur main).
>
> ⚠️ Le repo documente aussi désormais `qa/human-qa-playbook.md`, une doctrine
> de test "comme un humain" (clic/scroll/jugement visuel, plus de navigation
> par URL) qui **remplace conceptuellement** ce run URL-sweep pour les futurs
> agents QA manuels. Les runs du 2026-07-04, 2026-07-06 et 2026-07-07 (x2,
> 00:58 et 01:58) ont tous suivi la spec URL-sweep historique (v5 streaming)
> telle que demandée par le prompt de tâche reçu à chaque fois, pas le nouveau
> playbook — cette divergence n'est toujours **pas résolue par un humain**
> après 4 runs consécutifs ; si tu es un futur agent QA et que tu lis ceci,
> signale-le explicitement dans ton rapport plutôt que de trancher silencieusement.

Last run: 2026-07-07T04:10:45.000Z

## Faux positifs connus (méthode C)

| route | app | détecté le | cause | résolution |
| --- | --- | --- | --- | --- |
| /appointments | patient | 2026-06-25 | CSS Flutter `::placeholder` capté comme marker | faux positif — feature complète, ferme #2724 #2749 |
| /salle-attente | secretariat | 2026-06-25 | idem CSS Flutter | faux positif — ferme #2746 |
| /profile | patient | 2026-06-25 | idem CSS Flutter | faux positif — ferme #2728 |
| /messages | praticien | 2026-06-25 | idem CSS Flutter `flt-text-editing::placeholder` | faux positif — feature complète, ferme #2734 |
| /agenda | praticien | 2026-06-25 | idem CSS Flutter `flt-text-editing::placeholder` | faux positif — feature complète, ferme #2732 |
| /agenda | secretariat | 2026-07-01 | idem CSS Flutter `flt-text-editing::placeholder` | faux positif — feature complète (AgendaPage : liste des créneaux, filtre praticien, prise de RDV, confirmation ; câblée dans app_router.dart et ProShell.bodyBuilder), ferme #3172 |
| /notifications | patient | 2026-06-25 | déjà résolu — BlocBuilder exhaustif, try/catch, GetIt enregistré, flutter analyze vert | ferme #2833 |
| /notifications | patient | 2026-07-01 | idem CSS Flutter `flt-text-editing::placeholder` | faux positif — feature complète (NotificationsPage : états loading/error/empty/loaded, liste, marquage lu, tuiles ; route câblée dans app_router.dart avec BlocProvider > Scaffold > AppBar/body, GetIt enregistré), ferme #3168 |
| /bookable-slots | secretariat | 2026-06-30 | déjà résolu — BookableSlotsBloc wrap try/catch + SafeEmitMixin, BookableSlotsBody (BlocBuilder) couvre tous les states (Initial/Loading/Loaded vide/Loaded/Error via NubiaErrorWidget), route câblée dans app_router.dart (BlocProvider > BookableSlotsPage > Scaffold), bloc enregistré dans GetIt (pro_di.dart), 16 tests bloc+widget verts (bookable_slots_test.dart) | faux positif (frontend) — la régression backend réelle (405 sur GET /v1/cabinet/slots) documentée sous #3055/#3173 est désormais **corrigée** (voir note run 2026-07-04 ci-dessous) |
| /ordonnances | praticien | 2026-06-25 | déjà résolu — OrdonnancesPage, BlocBuilder exhaustif, GetIt, flutter analyze vert | ferme #2853 #2854 |
| [*] blank-canvas | ALL | 2026-06-27 | go_router path routing — location.hash ignoré (canvas blanc) | faux positif — ferme #2920-#2935. Naviger via location.hash (in-page, sans page.goto) |
| / et /account-setup | patient | 2026-07-01 | méthode C : scan DOM incluait le texte de balises `<style>` injectées (CSS `::placeholder`) via `.textContent` d'un `div` ancêtre | faux positif — ferme #3199 #3201. Fix harness : exclure `style`/`script` du scan DOM + filtre anti-CSS-boilerplate (`::placeholder`, `caret-color`, `-webkit-autofill`) |
| flow C (register praticien) | praticien | 2026-07-01 | déjà résolu — détections 20:25–20:36 antérieures au merge du fix #3194 (21:11, `context.go(cabinetSetup)` sur ProRegisterSuccess dans le listener du BlocConsumer) + follow-ups #3193 (08aac18b) et #3195 (7c4968b0, guestOnlyRoutes anti-course du guard) ; test widget de non-régression ajouté (pro_register_page_test.dart : ProRegisterSuccess → /cabinet-setup) | doublons périmés — ferme #3192 #3196 #3198 (le bug de fond récidivait à chaque run jusqu'au 2026-07-02 ; **flow C confirmé OK au run 2026-07-04**, voir note ci-dessous) |
| /messaging, /consultation, /devis, /login, /a2ui-demo | patient, praticien, secretariat | 2026-07-02 | périmé — détections blank-canvas des 29-30/06 (canvasCount:0) antérieures au run complet 2026-07-01T20:47 où TOUTES les routes des 3 apps sont OK ; même famille que #2920-#2935 (artefact harness) | doublons périmés — ferme #3039 #3046 #3056 #3079 #3092 |
| /waiting-room | praticien | 2026-07-02 | périmé — détection navigation du 01/07 (flutterViewPresent:false, pageTitle vide = page pas chargée, transient) ; route OK au run complet 2026-07-01T20:47 | doublon périmé — ferme #3135 |
| /messages | praticien | 2026-07-02 | doublon périmé — #3136 (kind navigation, run 2026-07-01T00:21:57Z, `page.goto: Target page, context or browser has been closed`) déjà documenté faux positif via #3145 (commit 3bfdd0fd, run antérieur 00:00:11Z) : CabinetMessagingBloc try/catch + SafeEmitMixin sur tous les handlers, CabinetMessagingPage switch exhaustif sur le sealed state (7 cas couverts), route wrappée Scaffold dans app_router.dart, bloc + use cases enregistrés GetIt (pro_di.dart, data_registration.dart), flutter analyze vert, 14 tests bloc+widget verts ; crash navigateur/contexte pendant le run, pas une exception app — même famille que #2734/#3135. Le seul finding distinct détecté depuis sur cette route (#3227, console-errors) a suivi son propre traitement, et s'est auto-résolu (voir run 2026-07-04) | doublon périmé — ferme #3136 |
| flow C step 1 | praticien | 2026-07-02 | déjà documenté faux positif usePathUrlStrategy (cf. notes méthodologiques : #3188-#3191 « postés puis fermés ») — #3190 resté ouvert par erreur ; les runs suivants passent l'étape 1 | faux positif — ferme #3190 |
| / (root) | patient | 2026-07-02 | déjà documenté — ligne « ferme #3199 #3201 » du 01/07 (méthode C, CSS ::placeholder) ; l'issue n'avait pas été fermée effectivement | faux positif — ferme #3199 |
| /reviews | patient | 2026-07-02 | même famille méthode C : marker = CSS `flt-text-editing::placeholder` capté dans le DOM, aucun hit grep source ; route OK au run 2026-07-01T20:47 | faux positif — ferme #3167 |
| /pharmacy/orders/:id, /patients/:id (praticien) | patient, praticien | 2026-07-04 | un ID de test factice/non-UUID produit une bannière "Impossible de charger..." attendue (400 côté API, erreur de parsing d'UUID) — comportement correct pour un ID inexistant, pas un bug | non applicable — comportement attendu, pas d'issue filée |
| /admin-membres | secretariat | 2026-07-04 | test avec le compte seed `sonia.accueil` (role=secretary) produit un 403 + bannière "Accès réservé aux administrateurs du cabinet." — gate de permission correctement implémentée, pas un bug | non applicable — comportement attendu, pas d'issue filée |
| /agenda, /liste-attente (secretariat) ; /patients/test-patient-id, /ordonnances, /ordonnances/new (praticien) ; /reset-password, /notifications (patient) | secretariat, praticien, patient | 2026-07-06 | seuil mécanique `whiteRatio > 0.995` déclenché sur des pages avec du **contenu réel** (agenda chargé après un délai réseau plus long que le sleep fixe du sweep, empty-states légitimes, bannière d'ID factice attendue, page à faible densité de texte) — voir note méthodologique whiteRatio ci-dessous | faux positifs — vérifiés à l'écran, aucune issue filée |
| /liste-attente (secretariat) ; /notifications, /reviews (patient) ; /patients/test-patient-id, /ordonnances, /ordonnances/new (praticien) ; /devis/test-devis-id, /admin-membres (secretariat) | secretariat, patient, praticien | 2026-07-06T21:05 | reconfirmation indépendante du run 18:24 le même jour, avec cette fois un décodage PNG correct (`pngjs`, canaux RGB réels) : whiteRatio mécanique `>0.995` sur pages à contenu réel (notification unique, empty-state légitime) + bannières d'erreur attendues sur ID factice / 403 rôle — tous vérifiés à l'écran, 0 issue filée | faux positifs — vérifiés à l'écran, aucune issue filée |

## Notes méthodologiques harness

- **app_practicien utilise `usePathUrlStrategy()`** (voir `front/apps/app_practicien/lib/bootstrap.dart:11`) — contrairement à patient/secretariat qui routent via `#hash`, praticien utilise des URLs "propres" (pathname). `window.location.hash` y est **toujours vide** ; utiliser `window.location.pathname` + `history.pushState()` + `dispatchEvent(new PopStateEvent('popstate'))` pour naviguer et vérifier la route courante sur cette app. Piège rencontré en Étape 4.5 (Flow C) : deux faux positifs `onboarding-flow-broken` postés puis fermés (#3188, #3189, #3190, #3191) avant correction.
- Formulaire `/register-pro` (praticien) : long formulaire scrollable — le dropdown "Spécialité" et le bouton "Créer mon compte" sont sous le fold à 800px de haut ; scroller `page.mouse.wheel(0, 700)` ×2 (avec `mouse.move` préalable pour cibler le glass pane) avant d'interagir. Faux positifs #3192, #3194, #3195 (jusqu'à interaction correcte).
- Bug réel confirmé (pas un faux positif) : `POST /v1/pro/register` répond 201 mais `ProRegisterPage`/`ProRegisterCubit` ne déclenche aucune navigation post-succès — voir #3198.
- **Run 2026-07-02T07:06 — la course register-pro récidive une nouvelle fois** malgré la fermeture de #3192/#3196/#3198 : POST 201 confirmé, mais le pathname final rebondit sur `/login` au lieu de `/cabinet-setup`. Non re-posté (dup <24h de #3198, fermé 05:49 le même jour) — à surveiller au prochain run une fois la fenêtre anti-doublon expirée si toujours reproduit.
- **Nouvelle classe de bug détectée le 2026-07-02 : bannière d'erreur visible en app malgré 0 console.error et 0 failedRequests** ("Erreur lors du chargement...", DioException probablement avalée par une course sur l'auth-interceptor/token-storage — famille déjà documentée sur #3199/#3167/#3135). Avait touché 8 routes patient (`/`, `/documents`, `/financial`, `/mes-rdv`, `/messaging`, `/notifications`, `/profile`, `/reviews`, issues #3216-#3223) et 2 routes praticien (`/`, `/messages`, issues #3225, #3227). Le harness classe ce cas comme finding même quand les critères mécaniques (whiteRatio/console/requests) indiqueraient un faux OK — toujours lire la capture d'écran. **Toutes ces routes sont revenues à un rendu réel et correct au run 2026-07-04** (contenu réel constaté à l'écran, pas juste critères mécaniques) — classe de bug résolue pour cette vague, mais le pattern lui-même (interceptor/token-storage race) reste à surveiller si il ressurgit.
- Onboarding flow B (secretariat, invitation) : fix confirmé au run 2026-07-02 (PR #3209 / commit `0264b7f5`) — token invalide correctement rejeté (400 `invitation_invalid`), écran "Invitation invalide" affiché. **Reconfirmé OK au run 2026-07-04** (la validation ne se déclenche qu'au submit du formulaire, pas au chargement de la page — le harness doit remplir+soumettre, pas juste naviguer).
- `/bookable-slots` (secretariat) : lacune backend réelle (405 sur `GET /v1/cabinet/slots`) confirmée à nouveau le 2026-07-02. **Corrigée au run 2026-07-04** : `api/src/lib.rs` enregistre désormais `get(scheduling::list_cabinet_slots).post(scheduling::create_cabinet_slot)` — vérifié en direct (grille de créneaux réelle affichée, 0 failed-request). Pas de nouvelle issue filée, régression résolue.
- **Run 2026-07-04T23:51 — Onboarding Flow C (register praticien) est OK pour la première fois après 6+ runs cassés.** Formulaire rempli intégralement (incl. dropdown Spécialité via activation de l'arbre sémantique), `POST /v1/pro/register` → 201, pathname atterrit correctement sur `/cabinet-setup`, formulaire cabinet-setup rendu. La course auth-guard documentée depuis #3192/#3193/#3195/#3196/#3198/#3203/#3204/#3205 ne s'est PAS reproduite ce run. **Une deuxième confirmation indépendante est recommandée avant de considérer la classe de bug définitivement résolue** (l'historique de faux "fix" cosmétiques sur cette route est long).
- **Run 2026-07-04 — nouveau bug bloquant sur Onboarding Flow A (patient signup) : `PATCH /v1/account` rejette systématiquement `birth_date` (422)**, alors que `AccountSetupPage` exige la date de naissance pour activer "Continuer" — bloque 100% des inscriptions patient à l'étape `/account-setup`. Root cause : `front/packages/nubia_data/lib/src/repositories/account_repository_impl.dart:35-37` envoie toujours `birth_date` dans le PATCH, alors que `api/src/auth/mod.rs:2087-2088` renvoie 422 dès que `birth_date` (ou `email`) est présent — cet endpoint est conçu pour ne jamais modifier ces champs. Ceci est un **bug distinct** de l'ancienne course auth-guard (#3100/#3022/#3072/#3025) — celle-ci semble résolue (l'agent atteint bien `/account-setup` sans rebondir sur `/login`), mais le nouveau bug empêche de compléter le flow. Tracké #3384 (P0).
- **Run 2026-07-04 — nouveau bug sur patient `/profile/dependents` : mismatch de nom de champ JSON.** Backend `DependentItem` (`api/src/auth/mod.rs:2957-2963`) sérialise l'id en `dependent_account_id`, mais `DependentDto.fromJson` (`front/packages/nubia_data/lib/src/remote/account/account_dto.dart:74`) lit `json['id']` → `TypeError` avalé en `ParseFailure` générique, bannière "Erreur de décodage de la réponse." malgré 0 console.error / 0 failedRequests (même famille que le bug interceptor déjà documenté, mais cette fois un vrai bug de contrat DTO, pas une course réseau). Tracké #3386 (P1).
- **Nouvelle feature découverte au run 2026-07-04 : "Ma pharmacie" côté patient** (`/pharmacy`, `/pharmacy/search`, `/pharmacy/send`, `/pharmacy/orders`, `/pharmacy/orders/:id`) — jamais explorée avant (absente du registre et du cap précédent), entièrement fonctionnelle, 0 bug, 0 marker feature-gap.
- **Nouvelle route découverte au run 2026-07-04 : praticien `/devis`** (plan de traitement) — absente du registre précédent, entièrement fonctionnelle.
- **Run 2026-07-04 — régression mineure sur praticien `/` : `GET /v1/cabinet/today-notes` → 404**, console.error déclenché, mais le widget concerné se dégrade proprement en état vide (contrairement à la bannière pleine page de l'ancien #3225, désormais résolu). Tracké #3383 (P1), sévérité moindre que l'ancien finding.
- **Run 2026-07-04 — 2 feature-gaps confirmés sur secretariat : `/admin-membres` et `/admin-secretariats`.** Les dialogues d'invitation (`invite_member_dialog.dart`, `invite_secretariat_dialog.dart`) affichent un faux SnackBar de succès ("Invitation envoyée (stub)") sans aucun appel backend réel — stub auto-documenté. Trackés #3381 et #3382 (P3). Distinct des anciens faux positifs CSS #3181/#3182 sur ces mêmes routes.
- **Piège harness confirmé au run 2026-07-04 (patient) : checkbox CGU Flutter.** Un `.click()` DOM brut sur `flt-semantics[role="checkbox"]` ne déclenche pas de vrai événement pointeur — utiliser le `.click()` natif Playwright sur le locator sémantique. Le champ "Date de naissance" est un date-picker Material (dialog), pas un input texte : ouvrir le dialog, basculer en mode "Passer à la saisie" texte, effacer la valeur pré-remplie avant de taper. **Ce piège n'est pas spécifique à `/signup` (patient)** : tout `Checkbox` Flutter web y est exposé, notamment `Key('onboarding_cgu_checkbox')` sur `/onboard` (secretariat, flow B) — un `.click()` DOM brut dessus laisse `_cguAccepted=false`, donc le bouton submit reste désactivé (`onPressed: null`), donc **aucune requête n'est envoyée et aucun message ne peut s'afficher**. C'est la cause la plus probable du finding "invalid-token message shown=false" du run 2026-07-06 (#3398) : vérifié que `AuthRepositoryImpl.register` mappe bien un 400 `invitation_invalid` vers `InvalidInviteFailure` et que `OnboardingPage` affiche l'écran "Invitation invalide" dès que `ProAuthCubit` émet `invalidInvite=true` — la chaîne applicative est intacte, donc si le message ne s'affiche pas c'est que le submit n'a jamais eu lieu côté harness.
- **Piège harness confirmé au run 2026-07-04 (praticien) : contamination de buffer console/réseau entre routes.** Une requête de la route précédente peut se résoudre pendant la fenêtre de capture de la route suivante si le settle-delay après login est trop court, produisant un faux `/devis` en `console-errors` — corrigé en isolant le re-test avec un délai de stabilisation plus long après login.
- **Run 2026-07-06 — le sandbox d'exécution n'a que 64 Mo de `/dev/shm`**, cause classique de crash du renderer Chromium headless sous charge WebGL/CanvasKit (`Target page, context or browser has been closed` en cours de run, parfois précédé de `WebGL: CONTEXT_LOST_WEBGL`). Lancer `chromium.launch({ args: ['--disable-dev-shm-usage', '--no-sandbox'] })` réduit la fréquence mais ne l'élimine pas totalement sur de longues sessions single-page multi-navigation — en cas de crash, ré-essayer la route dans un **navigateur/contexte frais** plutôt que de continuer sur la même page.
- **Run 2026-07-06 — le seuil `whiteRatio > 0.92` (v5 streaming, tel que spécifié) est essentiellement inutilisable tel quel sur cette UI.** Deux problèmes distincts : (1) calculer le ratio sur les **octets bruts du fichier PNG compressé** (DEFLATE) n'a aucune corrélation avec la couleur des pixels — il faut décoder le PNG (ex. `pngjs`) et lire les vrais canaux RGB ; (2) le fond d'app est `#fafaf9` (quasi-blanc) sur les 3 apps, donc **une page avec du contenu réel affiche déjà ~95-98 % de pixels quasi-blancs** (peu de texte/chrome sur un design minimaliste) — un seuil à 0.92 classifierait presque toutes les pages OK en faux `blank-canvas`. Ajusté empiriquement à un seuil interne `> 0.995` (au-dessus du bruit des pages réelles) **combiné à une relecture manuelle systématique de la capture** avant de conclure — jamais de POST automatique sur ce seul critère. `canvasCount` s'est avéré toujours égal à 0 sur les 3 apps (CanvasKit ne semble pas exposer de `<canvas>` compté par ce sélecteur ici) — signal à ne pas utiliser seul non plus.
- **Run 2026-07-06 — piège harness confirmé : `.click()` natif Playwright (pas seulement le `.click()` DOM brut déjà documenté) est parfois insuffisant sur `flt-semantics[role="button"|"checkbox"]`.** Root cause probable : le pointeur synthétique de Playwright n'active pas toujours le gesture recognizer de Flutter. Fix fiable : dispatcher soi-même la séquence `pointerover/pointerenter/pointerdown/pointerup/click` via `document.elementFromPoint(cx, cy)` au centre du `boundingBox()` (motif déjà utilisé dans `qa-verify-3398.js`, généralisé ici en helper `realClick()`). **Exception** : le bootstrap `flt-semantics-placeholder` (qui active l'arbre sémantique une seule fois par chargement de page) a besoin d'un vrai `.click()` DOM (`el.click()` via `page.evaluate`) — un dispatch de pointer-events dessus ne l'active pas.
- **Run 2026-07-06 — nouvelle classe de race harness : `NubiaTextField` recâble `onChanged: (_) => setState(() {})` sur toute la page à chaque frappe.** Un `.fill()` Playwright (instantané) peut arriver pendant un rebuild du sous-arbre sémantique déclenché par le champ précédent, et la valeur retombe silencieusement à vide (observé aléatoirement sur `Nom`, `Téléphone` selon les runs). Deux mitigations : (1) taper via `locator.pressSequentially(value, {delay:25})` puis **vérifier** `inputValue() === value` avec 3 tentatives (`fillFieldRobust()`) ; (2) cibler les inputs **par index** (`page.locator('input').nth(i)`), pas par label — `NubiaTextField` ajoute un suffixe hint/erreur au `aria-label` dès que le champ est non-vide (ex. "Mot de passe" → "Mot de passe\n8 caractères minimum..."), ce qui casse un `getByLabel(label, {exact:true})` capturé avant la frappe.
- **Run 2026-07-06 — le menu popup de `DropdownButton` (Spécialité, `/register-pro` praticien) n'est pas atteignable par un locator Playwright** (ni par rôle, ni par texte) — probablement peint uniquement en CanvasKit sans nœud sémantique séparé pour chaque item. Fix : navigation clavier une fois le menu ouvert (`ArrowDown` puis `Enter`), fiable à 2/2 runs consécutifs.
- **Run 2026-07-06 — nouveau bug bloquant sur Onboarding Flow A, distinct de #3384/#3397 (tous deux déjà fermés, tous deux à l'étape account-setup) : `PATCH /v1/account/coverage` rejette 422 dès que `mutuelle.amc` est renseigné sans `mutuelle.numero_adherent`**, alors que l'UI labelle explicitement ce champ "Numéro adhérent (**optionnel**)". Root cause : `PatchCoverageMutuelle` (`api/src/auth/mod.rs:2345-2349`) déclare `numero_adherent: String` (requis, pas `Option`), donc Serde rejette le body dès que `mutuelle` est présent sans les deux clés. Bloque l'étape finale du flow pour tout patient qui suit l'invite de l'UI. Tracké #3434 (P0) — **repris et fermé par flutter-agent en cours de ce même run** (PR #3435, fix frontend : envoie toujours `amc`+`numero_adherent` avec `?? ''` au lieu d'omettre la clé, + bannière d'erreur persistante au lieu d'un SnackBar transitoire). Root cause backend (champ non-optionnel malgré le contrat UI) non touchée par le fix — workaround frontend, à surveiller si une régression future réintroduit l'omission de la clé.
- **Run 2026-07-06 — Onboarding Flow C (register praticien) reconfirmé OK pour la 2e fois consécutive** (2 runs indépendants ce run, tous deux POST 201 → `/cabinet-setup` sans rebond sur `/login`). La course auth-guard documentée depuis #3192 et une longue lignée de faux "fix" semble bien résolue — recommandé de considérer cette classe de bug close sauf régression future.
- **Run 2026-07-06 — nouveau feature-gap praticien `/devis` (Méthode A, grep) : l'action "envoyer le devis au patient" est un mock local, aucun appel réseau.** `DevisBloc._onSendRequested` (`devis_bloc.dart:67-79`) émet `DevisSent` immédiatement après une mutation locale, sans jamais appeler `POST /v1/cabinet/quotes/:id/send` (qui n'existe d'ailleurs pas côté backend). Silencieux côté UI (rendu normal, aucun marker visible) — trouvé uniquement par grep source, pas par le scan DOM (Méthode C). Distinct de l'ancien #3370 (fermé, décrivait l'ancien écran 100 % stub list/detail, déjà remplacé par un vrai rendu depuis le 2026-07-04). Tracké #3436 (P3).
- **Run 2026-07-06T21:05 — le fix #3434/PR #3435 (coverage-setup 422 sur `numero_adherent` vide) tient à un run indépendant supplémentaire.** Flow A complet (signup → account-setup → coverage-setup → home) rejoué de bout en bout avec un compte jetable frais, aucun rebond, 0 console.error, 0 failed-request sur les 3 dernières étapes. Déploiement vérifié frais (`main.dart.js` Last-Modified 18:58-18:59Z, postérieur au merge 18:11Z de PR #3435).
- **Run 2026-07-06T21:05 — Onboarding Flow C (register praticien) reconfirmé OK pour la 3e fois consécutive** (indices de champ corrigés : Prénom(0) Nom(1) Email(2) MotDePasse(3) ConfirmMotDePasse(4) RPPS(5) ADELI(6) RaisonSociale(7) SIRET(8) — l'ordre réel du formulaire diffère de l'ordre supposé initialement ; un mauvais index remplit silencieusement le mauvais champ sans erreur de locator, produisant de faux échecs de validation qui ressemblent à un bug applicatif).
- **Run 2026-07-06T21:05 — piège harness : `page.getByLabel()` ne matche PAS un bouton Flutter dont le nom accessible vient de son texte (pas d'un `<label>`/`aria-label`).** Le champ "Date de naissance" (`/account-setup` patient) est un `FLT-SEMANTICS[role="button"]` sans `aria-label`, seulement du texte — `getByLabel(/date de naissance/i)` échoue silencieusement (isVisible=false), et le harness sautait alors tout le bloc de remplissage de la date sans erreur visible, laissant "Continuer" désactivé indéfiniment (faux `onboarding-flow-broken`, pas un bug produit). Fix : `getByRole('button', {name: ...})` pour ces éléments. Séquence complète confirmée : cliquer le bouton → cliquer "Passer à la saisie" → remplir l'`<input aria-label="Saisir une date">` qui apparaît (préremplie, à écraser) → "OK".
- **Run 2026-07-06T21:05 — #3436 (feature-gap praticien/devis, tracké plus tôt ce même run par Méthode A) a été repris et fermé par flutter-agent pendant que ce run était en cours** (PR #3440, `d0fb8913`, mergée 21:00:47Z, déployée 21:03:25Z) : `POST /v1/cabinet/quotes/:id/send` est désormais un vrai appel réseau. Non re-testé fonctionnellement faute de devis seed disponible sur le compte de test — à confirmer au prochain run.
- **Run 2026-07-06T21:05 — piège harness Méthode C (DOM feature-gap scan) : un `.replace(regex, '')` sans flag `/g` ne supprime qu'UNE occurrence du bruit CSS, laissant les occurrences suivantes de `::placeholder` déclencher un faux marker `placeholder` sur TOUTES les routes.** Le texte concaténé de `div` ancêtres contient souvent plusieurs blocs `<style>` injectés (un par route/composant Flutter re-rendu) — un seul `.replace()` non-global laisse les blocs suivants intacts. Fix : regex `/.../gi` + exclusion des éléments dont un descendant est `style`/`script` (`!e.querySelector('style, script')`) avant même de lire `.textContent`. Sans ce fix, le scan Méthode C aurait produit un faux feature-gap sur les 40 routes testées ce run.
- **Run 2026-07-06T22:41 — run complet, 40 routes (patient 16, praticien 11, secretariat 13) + 3 onboarding flows re-testés, 1 heure environ après le run précédent (21:05).** Résultat majoritaire : reconfirmation, aucune régression. Les 6 `blank-canvas` mécaniques (`whiteRatio>0.995`) et 2 `console-errors` mécaniques levés par le sweep (`/notifications`, `/reviews` patient ; `/patients/test-patient-id`, `/ordonnances`, `/ordonnances/new` praticien ; `/liste-attente`, `/devis/test-devis-id`, `/admin-membres` secretariat) ont tous été vérifiés à l'écran (screenshot) : mêmes valeurs de whiteRatio que les runs précédents, mêmes empty-states légitimes / bannières d'ID factice ou 403 attendues déjà documentées ci-dessus — 0 nouvelle issue filée sur ces critères mécaniques. Méthode C (DOM scan) : 0 hit sur les 3 apps. Onboarding flows A/B/C : les 3 OK (flow B avec le 400 `invitation_invalid` attendu sur token invalide, non compté comme finding).
- **Run 2026-07-06T22:41 — nouveau feature-gap réel confirmé sur patient `/rdv/test-appt-id/prepare` (Méthode A, grep source, distinct de l'ancien #3170 fermé comme faux positif CSS).** `PrepareRdvPage._load()` (`front/apps/app_patient/lib/features/mes_rdv/prepare_rdv_page.dart:80`) appelle inconditionnellement `_stubPreparation(widget.appointmentId)` — adresse et checklist ("Carte Vitale", "Carte mutuelle", "Ordonnance") codées en dur, identiques pour tout `appointmentId`, aucun appel réseau. Le backend `GET /v1/appointments/:id/preparation` existe et est implémenté (`api/src/lib.rs:307-308`, `api/src/appointments.rs:824`, issue #2469 fermée) mais `front/packages/nubia_data/lib/` ne contient aucune référence à `preparation` — repository/DTO jamais câblé côté frontend. Tracké #3441 (P3).
- **Tentative de nettoyage des comptes QA jetables créés pendant les flows A/C (Étape 4.5) : `DELETE /v1/account` répond 405 (méthode non supportée)** — pas de mécanisme de suppression de compte côté API actuellement. Les comptes `qa-patient-*@nubia.test` / `qa-praticien-*@nubia.test` s'accumulent donc à chaque run ; à surveiller si cela pose un problème de volumétrie plus tard (hors scope de ce run pour créer une issue backend, noté ici pour traçabilité).
- **Run 2026-07-07T00:58 — run complet de reconfirmation, ~2h15 après le run précédent (2026-07-06T22:41). 41 routes (patient 16, praticien 12, secretariat 13) + 3 onboarding flows, 0 régression, 0 nouvelle issue filée.** Toutes les valeurs whiteRatio mécaniques `>0.995` (patient `/pharmacy/orders/:id` 0.9971, `/rdv/test-appt-id/prepare` 0.9972 ; praticien `/patients/test-patient-id` 0.9974 ; secretariat `/liste-attente` 0.9962) correspondent à des bannières 400 UUID-factice attendues ou un empty-state déjà documenté — vérifiées à l'écran, mêmes valeurs qu'aux runs précédents. Méthode A (grep stub/TODO) et Méthode C (DOM scan) : 0 hit sur les 3 apps. Script sweep réécrit (`qa-run/sweep2.js`) avec décodage PNG réel via `pngjs` (comptage de vrais pixels RGB quasi-blancs, pas les octets bruts du fichier compressé) — cohérent avec la correction déjà en usage depuis le run 21:05 du 07-06, formalisé dans le script cette fois plutôt qu'ad-hoc.
- **Run 2026-07-07T00:58 — nouveau piège harness sur le dropdown Spécialité (`/register-pro` praticien, Onboarding flow C) : un locator `flt-semantics:has-text("Spécialité")` matche le `role="group"` racine du formulaire entier** (dont le texte concaténé contient tous les labels de la page, y compris "Spécialité"), pas le bouton du dropdown lui-même — cliquer dessus ne fait rien d'utile (centre du groupe = ailleurs sur la page). Le vrai déclencheur est un `flt-semantics[role="button"]` **distinct**, positionné juste sous le label (même `y` à ±30px, largeur du champ ~400px, texte vide). Fix : localiser le label exact (`textContent.trim() === 'Spécialité'`), puis chercher le `role="button"` voisin par proximité verticale plutôt que par `:has-text`, et cliquer ses coordonnées calculées. Sans ce fix, flow C se serait terminé avec le bouton "Créer mon compte" durablement désactivé (0 console.error, 0 failed-request — silence total, ressemble à un `onboarding-flow-broken` mais c'est un défaut de sélecteur, pas un bug produit) ; **une fois corrigé, flow C est OK — 4e confirmation indépendante consécutive**.

- **Run 2026-07-07T01:58 — run complet de reconfirmation, ~1h après le run précédent (2026-07-07T00:58), harness réutilisé tel quel (`qa-run/sweep2.js`, `qa-run/onboarding.js`) sans modification.** 41 routes (patient 16, praticien 12, secretariat 13) + 3 onboarding flows, 0 régression, 0 nouvelle issue filée. Les 5 consoleErrors/failedRequests mécaniques levés (patient `/pharmacy/orders/test-order-id` 400 UUID factice, `/rdv/test-appt-id/prepare` 400 UUID factice ; praticien `/patients/test-patient-id` 400 UUID factice ; secretariat `/devis/test-devis-id` 400 UUID factice, `/admin-membres` 403 rôle secretary) ont des messages d'erreur et whiteRatio **identiques au caractère près** aux runs précédents (mêmes textes `Invalid URL: UUID parsing failed` / `{"code":"forbidden"}`) — confirmés faux positifs déjà documentés, aucune capture ne montre de nouveau symptôme. Méthode A (grep TODO/stub sur `front/apps/*/lib/features/`), Méthode B (router-stub) et Méthode C (scan DOM) : 0 hit sur les 3 apps. Aucun code front/back n'a changé depuis le run précédent (`git log --since` sur nubiadoc et `/pulls?sort=recentupdate` sur Forgejo : dernier merge 2026-07-06T23:18Z, avant le run précédent) — ce run est donc une reconfirmation à isocode, valeur principalement en détection de régression d'environnement (déploiement, réseau), qu'on peut considérer nulle ici.
- **Run 2026-07-07T01:58 — divergence méthodologique non résolue, signalée comme demandé par la note d'en-tête : ceci est le 4e run consécutif (2026-07-04, 2026-07-06, 2026-07-07×2) à suivre la spec URL-sweep v5 streaming du prompt de tâche reçu, alors que le repo documente depuis `qa/human-qa-playbook.md` une doctrine de test "comme un humain" censée la remplacer conceptuellement.** Aucune décision humaine n'a tranché entre les deux. Recommandation explicite au lecteur (agent ou humain) de ce registre : soit mettre à jour le prompt de tâche automatisé pour pointer vers le playbook humain, soit documenter explicitement que l'URL-sweep reste la méthode voulue pour ce rôle récurrent — le statu quo silencieux fait perdre la valeur du playbook déjà écrit.
- **Run 2026-07-07T04:10 — run complet de reconfirmation, ~2h après le run précédent (2026-07-07T01:58), harness réutilisé tel quel (`qa-run/sweep2.js`, `qa-run/onboarding.js`) sans modification.** 46 routes (patient 21, praticien 12, secretariat 13) + 3 onboarding flows, 0 régression, 0 nouvelle issue filée. Confirmé qu'aucun commit n'a atterri sur `main` depuis le run précédent (`git log --since=2026-07-07T01:58:50` : vide ; dernier merge Forgejo #3442 à 2026-07-06T23:18:15Z, antérieur au run 01:58) — reconfirmation strictement à isocode. Les 5 consoleErrors/failedRequests mécaniques levés (patient `/pharmacy/orders/test-order-id` whiteRatio 0.9971, `/rdv/test-appt-id/prepare` 0.9972 ; praticien `/patients/test-patient-id` 0.9974 ; secretariat `/devis/test-devis-id` 0.9936, `/admin-membres` 0.9926) ont des messages d'erreur, statuts HTTP et whiteRatio identiques aux runs précédents (`Invalid URL: UUID parsing failed…` / `{"code":"forbidden"}`) — vérifiés à l'écran (captures `/tmp/qa-patient-reviews.png`, `/tmp/qa-secretariat-admin-membres.png` entre autres), aucun nouveau symptôme. Méthode A (grep TODO/FIXME/stub/placeholder sur `front/apps/*/lib/features/*`), Méthode B (router-stub `app_router.dart`) et Méthode C (scan DOM texte) : 0 hit sur les 3 apps (les 2 seuls hits grep, des `// TODO(nubia)` dans `session/pro_auth_cubit.dart` de praticien et secretariat, sont hors du périmètre Méthode A car situés hors de tout `features/<slug>/` rattachable à une route testée). Les 3 onboarding flows (A patient signup, B secretariat invitation invalide, C praticien register-pro) sont OK avec le harness existant sans aucune modification requise cette fois — confirmation supplémentaire que les pièges harness précédemment documentés (checkbox, dropdown Spécialité, indices de champ) restent correctement contournés. Divergence méthodologique (voir note du run 01:58 ci-dessus) toujours non tranchée par un humain — signalée à nouveau ici sans décision unilatérale de ma part.

## patient

| route | first_seen | last_check | last_status | last_finding |
| --- | --- | --- | --- | --- |
| / | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — 0 console.error, 0 failed-request, contenu réel affiché |
| /a2ui-demo | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /account-setup | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | Onboarding flow A : reconfirmé, formulaire soumis avec succès (compte jetable frais) |
| /appointments | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — carte + liste praticiens réelle |
| /book | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /coverage-setup | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | Onboarding flow A : reconfirmé — 0 console.error, 0 failed-request, atteint home après clic "Plus tard" |
| /documents | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, liste réelle |
| /financial | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, liste réelle |
| /forgot-password | 2026-07-01 | 2026-07-06T18:24:08.000Z | OK | reconfirmé (contexte non-authentifié, non re-testé ce run, hors cap) |
| /login | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé (contexte non-authentifié, via Onboarding flow A step 1) |
| /mes-rdv | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /messaging | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, liste réelle |
| /notifications | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé (whiteRatio mécanique 0.9966 faux positif, valeur cohérente avec les runs précédents — 1 notification réelle affichée) |
| /oubliettes | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, feature-gap #3224 toujours corrigé |
| /pharmacy | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /pharmacy/search | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, annuaire OK |
| /pharmacy/send | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /pharmacy/orders | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, liste OK |
| /pharmacy/orders/:id | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | bannière "Impossible de charger..." avec ID factice = comportement attendu (400 UUID parsing, reconfirmé, vérifié à l'écran) |
| /profile | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /profile/dependents | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, 0 console.error |
| /profile/consents | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /profile/notifications | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /rdv/test-appt-id/prepare | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | runtime OK (bannière 400 UUID factice attendue, vérifié à l'écran) ; feature-gap #3441 toujours corrigé |
| /reset-password | 2026-07-01 | 2026-07-06T18:24:08.000Z | OK | reconfirmé (contexte non-authentifié, non re-testé ce run, hors cap) |
| /reviews | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé (whiteRatio mécanique 0.9972 faux positif — empty-state légitime "Aucun avis pour ce prestataire", vérifié à l'écran) |
| /signup | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé (contexte non-authentifié + via Onboarding flow A) |
| /splash | 2026-07-01 | 2026-07-06T21:05:13.000Z | OK | reconfirmé (contexte non-authentifié) |

## praticien

| route | first_seen | last_check | last_status | last_finding |
| --- | --- | --- | --- | --- |
| / | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — 0 console.error, 0 failed-request |
| /a2ui-demo | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /agenda | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /cabinet-setup | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, atteint via Onboarding flow C (6e confirmation indépendante) |
| /consultation | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | runtime OK ; feature-gap déjà tracké — #3226 |
| /devis | 2026-07-04 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — feature-gap envoi devis (#3436/PR #3440) tient, rendu OK, 0 marker méthode A |
| /login | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé via login flow |
| /messages | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, liste de conversations réelle |
| /ordonnances | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — état vide légitime "Aucune ordonnance en cours" (whiteRatio mécanique 0.9955 faux positif, vérifié à l'écran) |
| /ordonnances/new | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — état vide légitime "Nouvelle ordonnance" (whiteRatio mécanique 0.9956 faux positif, vérifié à l'écran) |
| /patients | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /patients/test-patient-id | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | bannière "Impossible de charger le patient." avec ID factice = comportement attendu (400 UUID parsing, reconfirmé, vérifié à l'écran) |
| /register-pro | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | rendu + flow de soumission complet OK, **6e confirmation indépendante consécutive** |
| /splash | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé via login flow |
| /waiting-room | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |

## secretariat

| route | first_seen | last_check | last_status | last_finding |
| --- | --- | --- | --- | --- |
| / | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, KPI + états vides corrects |
| /a2ui-demo | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /admin-membres | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | runtime OK (403 attendu, rôle secretary, vérifié à l'écran) ; feature-gap #3381 toujours corrigé |
| /admin-secretariats | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | feature-gap #3382 toujours corrigé |
| /agenda | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /appointments | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /bookable-slots | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, grille de créneaux réelle |
| /devis | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /devis/test-devis-id | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | bannière 400 UUID invalide avec ID factice = comportement attendu (reconfirmé, vérifié à l'écran) |
| /liste-attente | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé — état vide légitime "Pas d'attente" (whiteRatio mécanique 0.9962 faux positif, vérifié à l'écran) |
| /login | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé via login flow |
| /messages | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /onboard | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | invitation_token invalide → "Invitation invalide" (reconfirmé, voir Onboarding flow B) |
| /patients | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé, cloisonnement clinique respecté |
| /salle-attente | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé |
| /splash | 2026-07-01 | 2026-07-07T04:10:45.000Z | OK | reconfirmé via login flow |

## Onboarding flows (Étape 4.5)

| flow | app | last_check | last_status | last_finding |
| --- | --- | --- | --- | --- |
| A (signup patient) | patient | 2026-07-07T04:10:45.000Z | OK | reconfirmé de bout en bout (signup → account-setup → coverage-setup → home) avec un compte jetable frais : 0 console.error, 0 failed-request. |
| B (invitation secretariat) | secretariat | 2026-07-07T04:10:45.000Z | OK | reconfirmé — invitation_token invalide correctement rejeté au submit, "Invitation invalide" affiché, bouton "Retour à la connexion" fonctionnel. |
| C (register praticien) | praticien | 2026-07-07T04:10:45.000Z | OK | **6e confirmation indépendante consécutive** — POST 201 → /cabinet-setup atteint sans rebond sur /login, run indépendant ~2h15 après le précédent. |
