\* Convergence config: validate the spec is well-formed and that the
\* _isRefreshing guard provides mutual exclusion. Both invariants below are
\* EXPECTED TO HOLD. NoLostRetry is intentionally NOT checked here (it is the
\* bug-hunting target — see MC_hunt_refresh.cfg).
SPECIFICATION Spec

CONSTANTS
    Requests = {r1, r2, r3}

INVARIANT TypeOK
INVARIANT SingleRefreshInFlight
