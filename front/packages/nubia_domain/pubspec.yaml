name: nubia_domain
description: Pure Dart domain layer (entities, value objects, repository ports, use cases, failures).
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  equatable: ^2.0.7
  dartz: ^0.10.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
