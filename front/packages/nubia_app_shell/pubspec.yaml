name: nubia_app_shell
description: Shared pro-app scaffold — NavigationRail desktop / Drawer mobile + clinical guard.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  go_router: ^15.1.0
  nubia_core:
    path: ../nubia_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
