name: nubia_core
description: Cross-app infrastructure — Dio client, JWT interceptor, storage, DI, router primitives, auth session.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0
  get_it: ^8.0.0
  go_router: ^15.1.0
  flutter_secure_storage: ^9.2.4
  url_launcher: ^6.3.1
  nubia_domain:
    path: ../nubia_domain

dev_dependencies:
  flutter_lints: ^5.0.0
