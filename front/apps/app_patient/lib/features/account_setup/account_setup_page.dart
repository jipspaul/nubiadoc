import 'package:flutter/material.dart';

/// Placeholder — profile completion logic added in P4.
class AccountSetupPage extends StatelessWidget {
  const AccountSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Configurer votre compte')),
      body: const Center(
        child: Text('Configuration du compte (à venir)'),
      ),
    );
  }
}
