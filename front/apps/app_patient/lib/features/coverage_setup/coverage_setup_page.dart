import 'package:flutter/material.dart';

/// Placeholder — couverture setup logic added in a later issue.
class CoverageSetupPage extends StatelessWidget {
  const CoverageSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Configurer votre couverture')),
      body: const Center(
        child: Text('Configuration de la couverture (à venir)'),
      ),
    );
  }
}
